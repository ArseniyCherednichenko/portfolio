// The build log for this site.
//
// This portfolio is open source and grows in the open, one coherent
// improvement at a time — the commit history is part of the story. Rather than
// fake a precise per-day changelog, this file gathers the real work into
// honest chapters: what actually shipped, grouped by the phase it belongs to.
// Newest chapter first. Kept current by hand (and by the daily routine) as the
// site keeps moving.

export type EntryKind = 'page' | 'component' | 'motion' | 'polish' | 'infra'

export interface LogItem {
  kind: EntryKind
  text: string
}

export interface Chapter {
  /** Short marker shown on the spine, e.g. "05" or "Now". */
  marker: string
  /** Chapter title. */
  title: string
  /** One or two honest sentences framing the chapter. */
  summary: string
  /** The concrete things that shipped in this chapter. */
  items: LogItem[]
}

// Human labels + accent treatment for each kind of entry. Used by the filter
// and the little tag chips.
export const KIND_META: Record<EntryKind, { label: string; hint: string }> = {
  page: { label: 'Page', hint: 'a new route or view' },
  component: { label: 'Component', hint: 'a reusable building block' },
  motion: { label: 'Motion', hint: 'animation and interaction' },
  polish: { label: 'Polish', hint: 'refinement and detail' },
  infra: { label: 'Infra', hint: 'build, types, plumbing' },
}

export const KIND_ORDER: EntryKind[] = ['page', 'component', 'motion', 'polish', 'infra']

export const CHAPTERS: Chapter[] = [
  {
    marker: 'Now',
    title: 'Saying it plainly',
    summary:
      'After a long run of building the craft, a turn back to the person behind it: stating the beliefs the work runs on, in plain words, and de-centring any single project so the site reads as him and not one product.',
    items: [
      { kind: 'component', text: "Combobox — the Select's editable twin, and the control the family had been missing: not the listbox you open and scan, but the autocomplete you type into to narrow a long list down to the one thing you meant. That is a different, harder contract, so it is built to the WAI-ARIA editable-combobox pattern rather than dressed up to look like one — a real text input owns the focus and drives a filtered listbox beneath it through aria-activedescendant, a polite live region announces how many options the current query leaves so a screen reader hears the list shrink, and the full keyboard run works: the arrows and Home/End move the highlight, Enter or Tab completes the highlighted option, and Escape clears the field or closes the panel. The matching is a subsequence, not a prefix, so a loose query like 'remo' still finds Framer Motion and 'tywr' still finds Tailwind — every result underlines the exact letters the query hit, scored so the tightest, earliest matches rank first. On that honest foundation sits the motion: the panel springs open with a small blur-and-rise, the single lime highlight glides between results on a shared layoutId instead of blinking from row to row, and a clear button folds in the moment there is text to clear. It debuts in the Playground's Navigation & controls band, right after the Select menu, wired to a live readout that names the chosen tool, with one option left disabled to show the state. Under prefers-reduced-motion the spring, the blur, and the gliding highlight all come off for a plain, instant, equally usable field. Catalogued in the library under Navigation & controls; the component count moves to 157." },
      { kind: 'component', text: "Colour picker — the control the browser buries in an operating-system dialog you can never style, place, theme, or animate, so it looks different on every machine. Rebuilt here from the HSV colour maths up, and a deliberate turn back to product-grade interface craft. The canonical state is kept in hue, saturation, and value rather than hex, because a hex round-trip loses the hue the moment a colour hits black or a pure grey — so dragging the thumb into a dark corner and back out never makes the plane or the rail jump. Four ways to set a colour, all live: drag the thumb across the saturation-and-brightness plane, slide the rainbow hue rail beneath it, type a hex straight into an editable field that sanitises and clamps what you paste, or tap one of the presets; and where the browser supports the EyeDropper API — feature-detected, so the control never promises what the engine can't do — a screen sampler lifts the exact colour of any pixel on your display. The plane and the hue rail are real role=slider controls, not just draggable divs: focus either and the arrow keys drive it — on the plane Left and Right move saturation while Up and Down move brightness, Home and End pin saturation to its ends, and Shift takes bigger steps — with each surface reading its live hex and channel values to a screen reader. A preview panel beside the picker spends the chosen colour as a working accent — a tinted card, a solid and a ghost button, a status dot, a notification glow — so the value is felt as something a product would use, not just read as six hex digits, and an honest rgb() read-back sits below. It debuts full-width in the Playground's Navigation & controls band, before the Dynamic island, and is catalogued in the library under Navigation & controls; the component count moves to 156. Under prefers-reduced-motion the thumb's grab-swell and the gliding preset check come off for a plain, equally usable picker." },
      { kind: 'component', text: "Date picker — the control famous for being the hardest of all to build well, and a return to product-grade interface craft after the run of honest-physics toys. The native date input can't be styled, and every design system ends up rebuilding it, because a calendar is not a menu like the Select before it — it is a two-dimensional grid you navigate like a spreadsheet, and the month underneath you has to slide away when you cross its edge. So this is built to the WAI-ARIA date-grid contract, not a stack of divs: a real role=grid of weeks and gridcells with a single roving tabindex, so Tab lands on the grid exactly once and the arrow keys do the rest — Left and Right step a day, Up and Down a week, Home and End jump to the week's ends, PageUp and PageDown change the month, and holding Shift turns those into a year — with Enter or Space to choose, and crossing a month boundary flips the view so a person can walk from the end of March straight into April without lifting their hands. The month arithmetic is honest to a person's expectations: adding a month to the 31st lands on the last valid day of the shorter month rather than spilling forward, and the six-row matrix is padded with the tail and head of the neighbouring months, greyed, so every week is full and the grid never reflows. Every gridcell is a real button that speaks its own full date to a screen reader, marks aria-selected on the chosen day and aria-current=date on today, and after any keyboard move DOM focus is pulled onto the newly focused day so the roving tabindex and the visible focus ring stay in lockstep. On that honest foundation sits the motion the native control can never have: the month grid slides in the direction you travelled — forward slides in from the right, back from the left — the month title cross-fades on the same axis, and the lime selection pill glides from the old day to the new one on a shared layoutId scoped to the visible month, so within a month the selection travels rather than blinking, while a month change cleanly re-forms it. Today wears a small lime dot; the chosen day, the filled lime pill with black figures. It is controlled or uncontrolled, and it debuts full-width in the Playground's Navigation & controls band, before the Dynamic island, wired to a live readout that names the chosen day in full through Intl so the value is felt, not just seen. Under prefers-reduced-motion the slide, the title cross-fade, and the gliding pill all come off for a plain, instant grid that is exactly as usable. Catalogued in the library under Navigation & controls; the component count moves to 155." },
      { kind: 'component', text: "Double pendulum — the smallest machine that refuses to be predicted, and a turn back to honest physics after the run of interface and generative work. One arm hangs from a fixed pivot; a second hangs from the tip of the first. Two rods, two weights, one law of gravity, and the whole thing is deterministic to the last decimal — and yet no one can tell you where the lower weight will be a minute from now, because the equations that govern it are chaotic: change the starting angle by a hair and the paths diverge into completely different lives. That sensitive dependence on initial conditions is the piece's whole argument, so it does not show one pendulum, it shows a family of eleven, all released from all-but-identical starts, and lets you watch the family come apart. For the first few seconds they swing as one, a single arm; then the hairline differences amplify, the arms peel away from each other, and a tidy line of clones fans into chaos. The physics is honest, not a plausible fake: each clone integrates the real double-pendulum equations of motion — the full coupled expression, θ measured from the downward vertical — with a fourth-order Runge-Kutta integrator on a small fixed timestep, an accumulator eating the rAF delta in 1/240-second bites, so the simulation is frame-rate independent and numerically stable and the divergence you see is the true physics rather than a rendering trick. The only liberty is a whisper of damping, the air resistance a real pendulum feels, which keeps an always-on decorative loop from drifting to infinity over a long visit. The hairline offsets that separate the clones come from a fixed integer hash of each one's index, so the same family diverges the same way every load and it never touches Math.random; time is the rAF delta alone, never Date.now, so it is resize-stable and never trips the environment's guards. Each weight's tip draws a short, fading trail, so the fan leaves a bloom of arcs behind it — the signature scribble of chaos — and the leading clone is drawn brightest so the eye has an anchor while the crowd fans out in graphite behind it. It is live under the pointer: press anywhere and the whole family re-releases from rest, every arm pointing at the cursor, and falls again from there. It sits among the honest-physics toys — the coupled counterpart in spirit to the Galton board's genuine per-strike chaos — and debuts as a full-width Objects & toys experiment in the Playground, after the slime colony. Under prefers-reduced-motion the loop never starts: a single settled pendulum is laid out once with a few faint ghosts behind it to hint at the family, and held perfectly still. Catalogued in the library under Objects & toys; the component count moves to 154." },
      { kind: 'component', text: "Select — the one control every real form leans on, hand-built because the native <select> can't be styled or animated past its box, and a deliberate turn from the recent run of generative canvas fields back to product-grade interface craft. It is a real accessible listbox, not a div dressed up as one: it follows the WAI-ARIA select-only combobox pattern, where the trigger keeps keyboard focus and drives the floating list through aria-activedescendant, so screen-reader and keyboard users get the full contract — open with Enter, Space, or the arrows; move the highlight with Up/Down and Home/End; type a letter or two to jump to a matching option; Enter or Space to choose; Escape to close; click outside to dismiss. On top of that honest foundation sits the motion the native control can never have: the panel springs open with a small blur-and-rise from its top edge, a single lime highlight glides between options on a shared layoutId instead of blinking from row to row, the chevron rotates as it opens, and the chosen option carries a check that scales in. It debuts in the Playground's Navigation & controls band, after the rotary knob, wired to a live preview — picking a typeface restyles the sample line beneath it, and one option is left disabled to show the state. Under prefers-reduced-motion the spring, the blur, and the gliding highlight all come off for a plain, instant, fully usable menu. Catalogued in the library under Navigation & controls, taking the count to 153 hand-built components." },
      { kind: 'component', text: "Slime — a Physarum simulation, and the Objects & toys family's answer to the question the boids beside it don't ask: how does a thing with no brain, no map, and no centre build a network that looks designed? It rebuilds the famous experiment where a slime mould, given oats laid out like the towns around Tokyo, grew a transport network that matched the city's rail map. Every agent here is almost nothing — a position, a heading, and one instinct: sniff the trail just ahead, a little to the left and a little to the right, turn toward whichever smells strongest, take a step, and leave a little scent where it lands. That is the whole program. Thousands of them run it at once on a coarse scent field that softly blurs and decays every frame, and out of it assembles the reticulate, vein-like mesh a real slime mould grows to connect its food — emergence you can watch build itself, with no brain, no map, and no centre anywhere in it. It sits apart from the physics all around it: not a crowd of bodies pushing on one another like the Ballpit or the Cradle, but a swarm reading and writing a shared chemical field, closer in spirit to the Chladni plate's particles-reading-a-field than to any of the collision toys. Nothing is placed by hand: every agent's start and heading, and the small wander it takes when the trail ahead is flat, come from a fixed integer hash of its index and the step count, so the same network grows every load and it never touches Math.random or Date.now. Under the hood it is one coarse scent grid held in two Float32 buffers swapped each frame — deposit, then a 3x3 box-blur-and-decay — with the field mapped through a black-to-teal-to-lime ramp and blitted up soft, which is what gives the veins their glow; all of it in refs on the hot path, DPR-aware, and re-seeded deterministically on resize. The pointer is food: move across it and scent pools under your cursor so the veins reach toward you and thicken; press to drop a richer bloom the colony races to colonise. It debuts full-width in the Playground's Objects & toys, before the abacus, and is catalogued in the library, taking the count to 152 hand-built components. Under prefers-reduced-motion the loop never starts — the colony is stepped forward to a settled network, drawn a single time, and held still, re-settling only on resize." },
      { kind: 'page', text: "Taste — a new page, /taste, that stops describing the site's central promise and shows it. The whole site keeps saying I sweat the small moments no one notices but everyone feels; this page renders four ordinary pieces of interface — a button, an empty state, a number, a form field — twice each: Plain, the honest default anyone would ship, and Considered, the same thing after the care. A single segmented switch flips every demo at once, so the taste reads as the delta between the two states rather than as a claim, and each card keeps an always-visible note naming exactly what changed and why it matters (a focus ring you can find by keyboard, tabular figures that hold still, an empty screen that reassures and points the way, one honest line of help before a mistake). It is deliberately its own thing among the craft pages: /craft demonstrates motion mechanics, /design catalogues static tokens, /playground is a gallery of components — this is applied judgement on the plain product surfaces that never make a showreel. It de-centres Guided entirely and invents nothing: every demo is a live component, not a testimonial or a metric. The mode switch glides on a shared-layoutId spring and each demo cross-fades on a fixed-height stage so the flip never jumps the layout; under reduced motion the glide and cross-fades drop to instant swaps and the one idle pulse holds still. Wired in the full way every page is — a go-chord (g then f), the command palette, the terminal's page list, the footer, and the index — and counted, taking the site to 27 pages." },
      { kind: 'component', text: "Fractal tree — a thing grown rather than drawn, and the Objects & toys family's quiet answer to all the physics beside it. Every other piece there is a crowd of bodies pushing on one another; this is one rule, applied to itself, all the way down. A branch is a line; at its tip it forks into two shorter branches turned a little to either side, and each of those does the same, and the same again, nine times over, until a whole crown falls out of a single instruction. It is recursion made visible — the motif a programmer reaches for a dozen times a day, standing here as a tree. Nothing about the shape is placed by hand: the fork angles and the length of each child are jittered per branch by a fixed integer hash of that branch's position in the tree (leftId = id·2, rightId = id·2+1 from a root of 1), so the crown is asymmetric and organic yet perfectly deterministic — the same tree every load, at every size. It grows on arrival, a growth clock opening the trunk first and each deeper ring of branches a beat later, so the tree unfurls from the ground up, then settles and breathes. The breathing is honest wind: each branch sways on its own sine, and because every child is drawn from its parent's already-swayed direction the sway compounds down the tree the way it does in a real one — the trunk barely stirs while the outermost twigs whip. It is live under the pointer: move across it and the whole crown leans the way you push, harder the further out; press to send a gust through it, and the buds at the tips warm to lime and pulse, a subset of them flowering brighter. No Date.now and no Math.random anywhere — the growth and sway run off the rAF delta and every jitter comes from the seeded hash — so it is resize-stable and never trips the environment's guards. One canvas, one loop, the whole tree recomputed each frame from its single rule. It debuts as a full-width Objects & toys experiment in the Playground, after the word sphere. Under prefers-reduced-motion the loop never starts: the finished tree is laid out once with a gentle fixed lean and held perfectly still, every branch and bud in place. Catalogued in the library under Objects & toys; the component count moves to 151." },
      { kind: 'component', text: "Word sphere — the old tag cloud, rebuilt as honest 3D, and a turn away from the physics toys toward the person: it is the tools Arseniy actually reaches for, laid out on the surface of a globe and turning slowly in space. The words sit on a Fibonacci lattice — points walked around the golden angle so they spread evenly over the sphere with no clumping at the poles, the same trick that scatters seeds on a sunflower — and each frame the whole cloud is rotated by two angular velocities, yaw around the vertical axis and pitch around the horizontal, then every point is perspective-projected to the plane. Front words swell, brighten, and lift above their neighbours; back words shrink, dim, and blur, so depth reads without any z-buffer, and the one nearest the viewer catches the lime accent. It is not a background image faking three dimensions: every word is a live DOM node — real, selectable text, read to a screen reader as a plain unordered list while the visual cloud stays decorative — and the transforms are written straight to the DOM in the rAF loop, so React renders the words exactly once. It is alive on arrival, drifting on a gentle frictionless idle spin, and steerable: move the pointer across it and the sphere turns to follow like a trackball, faster the further you push from the centre, then eases back to its drift when the pointer lifts; hover a single word and the whole sphere slows so you can read it. No wall clock and no randomness in the loop — the layout seed is fixed and deterministic — so it is resize-stable and never trips the environment's guards. It debuts as a full-width Objects & toys experiment in the Playground, after the Galton board. Under prefers-reduced-motion the loop never starts: the globe is laid out once, tipped to a readable three-quarter angle, and held perfectly still, every word legible. Catalogued in the library under Objects & toys; the component count moves to 150." },
      { kind: 'component', text: "Galton board — Francis Galton's 1873 bean machine, rebuilt with the physics kept honest, and the Objects & toys family's answer to a plain histogram: instead of stating that a set of numbers is normal, it lets the distribution precipitate out of the physics in front of you. Pour beads in at the top and each one clatters down through a staggered triangular lattice of pegs, taking a hair of a swerve left or right at every peg; do that over eight rows and a bead's landing bin is the sum of eight independent left-or-right nudges — a binomial — so a whole crowd of beads, dropped one after another, piles up into the bell curve nobody drew. The physics is honest, not scripted. Each bead is a point mass under gravity integrated by semi-implicit Euler at a fixed substep, and a peg is a real circle: a contact is resolved the textbook way — push the bead back out along the contact normal and reflect the normal component of its velocity with a little restitution — plus a whisper of tangential kick, because no real bead strikes a peg dead-centre and that sensitivity to where it lands is the whole point. The bead's swerve is genuine chaos seeded by the strike, not a coin flipped in code, which is why the binomial emerges rather than being assigned. Below the pegs the beads fall between thin dividers into their bins and stack up, so the pile heights are the histogram, drawn as they grow; when the tallest column fills its bin the board sweeps clean and begins the curve again, so it is never done. It is the moving counterpart to the column chart over on the numbers page. It is alive on arrival — it mounts with the bins already leaning toward the binomial shape and beads falling — and fully live under the pointer: press to pour a stream of beads from wherever you touch, so you can aim them and lean the pile off-centre, then release and watch the steady centre drip pull it back toward the bell. No wall clock and no seeded RNG on the hot path beyond the honest per-strike jitter — the loop runs off the rAF delta — so it is resize-stable. Every bead lives in a reused pool, one canvas, one loop, drawn additively so a live bead blooms against the settled piles. It debuts as a full-width Objects & toys experiment in the Playground, beside the Chladni plate. Under prefers-reduced-motion the loop never starts: the bins are filled to the exact binomial(8, ½) silhouette and painted still, so the point reads at a glance. Catalogued in the library under Objects & toys; the component count moves to 149." },
      { kind: 'component', text: "Chladni plate — Ernst Chladni's 1787 acoustics demonstration, rebuilt in maths, and the odd one out in the Objects & toys family. Every other physics piece there is a crowd of bodies pushing on one another — the Ballpit's spheres, the Cloth's nodes, the Cradle's bobs, the Murmuration's birds. This one is particles reading a field none of them can see. On a real bowed metal plate the sand flees the parts that shake hardest and gathers along the lines that barely move at all, so the grains draw the plate's standing-wave pattern for you; drive a different frequency and a different figure appears. The model here is the honest, standard one: for a square plate the vibration amplitude at a point is s = sin(pi*n*x)*sin(pi*m*y) + sin(pi*m*x)*sin(pi*n*y) over the unit square, and the nodal lines where s equals zero are the still places. Each grain takes a random step whose length grows with how hard the plate shakes beneath it — out on an antinode it is flung about and can never settle, near a nodal line it barely moves — so over a few hundred frames the whole crowd of twenty-six hundred grains drains onto the lines and the figure precipitates out of pure noise. Nothing places the pattern; it is the random walk biased by the field. It is alive on arrival — it mounts already sweeping slowly through a curated ring of real modes, easing the two mode numbers between integer figures so the lines re-thread rather than snap — and fully live under the pointer: move across the plate to tune it by hand, left-right setting one mode number and up-down the other, and the sand re-forms into whatever figure you land on; press to strike the plate and toss the settled sand back up so it re-gathers. No Date.now and no wall clock — the sweep advances off the rAF delta — so it is resize-stable. Every grain lives in flat typed arrays, one canvas, one loop, drawn additively so the sand reads brightest where it piles onto a line. It debuts as a full-width Objects & toys experiment in the Playground, beside the Newton's cradle. Under prefers-reduced-motion the loop never starts: one figure is settled in a tight batch of up-front iterations and painted still. Catalogued in the library under Objects & toys; the component count moves to 148." },
      { kind: 'component', text: "Newton's cradle — the desk toy, rebuilt with the physics kept honest, and the coupled counterpart to the Pendulum wave the site already has. Both are lines of pendulums, but they tell opposite stories: the wave's pendulums are independent, each tuned to its own rate so the eye reads the drift between them; the cradle's touch, hung to just kiss at rest, so the whole story is the handoff of a single blow straight down the line. The famous behaviour — lift one bob and one leaps off the far end, lift two and two leap, the middle three never stirring — is not scripted here; it falls straight out of the rule. Each bob is a real simple pendulum integrated by semi-implicit Euler at a fixed substep (α = -(g/L)·sinθ), and a contact is resolved the textbook way: when two neighbours on equal strings overlap, equal masses simply swap their velocities and are nudged back to the point of contact so they never stick together. Sweeping that resolver along the row a few times per frame lets one impact ripple through the entire chain within a single frame, so one-in-one-out and two-in-two-out emerge from the physics rather than being drawn by hand. It is alive on arrival — it mounts already swinging and runs all but frictionless, so it keeps going — and fully live under the pointer: drag either end bob to lift it while the rest hang still under your hand, then release to let it fall, and a struck bob briefly sparks so the blow is easy to follow. No Date.now and no Math.random anywhere — the loop runs off the rAF delta — so it is resize-stable and never trips the environment's guards. It debuts as a full-width Objects & toys experiment in the Playground, beside the Harmonograph. Under prefers-reduced-motion the loop never starts: one bob is painted caught mid-fall and held. Catalogued in the library under Objects & toys; the component count moves to 147." },
      { kind: 'component', text: "Voronoi — a living tessellation turned into a pointer field, and a turn back from four charts in a row to the generative-field craft the site is built on. It is one of those structures that shows up everywhere once you know its shape: scatter a set of seeds across a plane and the plane divides itself, every location claimed by whichever seed is nearest, and the borders between those territories are the Voronoi diagram — the geometry behind how cells pack, how cracks run, how crystals grow, how a phone finds its closest tower. Here the seeds are alive: each drifts on its own fixed heading and bounces off the four walls, so the borders breathe and re-knit continuously without ever tearing. It is fully live under the pointer — the cursor is a seed too, so moving it forms a cell that travels with you and shoves its neighbours aside, and pressing drops a fresh drifting seed so the whole tessellation re-partitions around the new point, the oldest recycled once a cap is reached. The rendering is the interesting part: it skips polygons entirely and reads the distance field. For every point on a coarse grid it measures the distance to the nearest seed and to the second nearest — those two are very different deep inside a cell and exactly equal on a border — so their difference is a signed distance to the closest Voronoi edge, and a smoothstep of it lights the seams as clean lime lines with no polygon clipping and no aliasing, while a soft core glows where the nearest distance falls to zero at each seed. It holds to the site's single-hue rule: everything is lime, only brightness carries the structure, tipping toward white where seams cross and at the seed cores so intersections read as sparks. Painted to a grid-sized offscreen buffer at one pixel per cell and smoothed up to full width by the GPU, so the hot loop weighs about twelve thousand cells against thirty seeds rather than a million pixels. No Date.now and no Math.random on the hot path — every seed's heading is fixed by a seeded PRNG at layout — so it is stable across resizes and never trips the environment's guards. It debuts as a full-width Pointer fields experiment in the Playground, right after the Game of Life, so the discrete automaton and the continuous partition read against each other. Under prefers-reduced-motion nothing moves: a seeded arrangement is painted once and held still. Catalogued in the library under Pointer fields; the component count moves to 146." },
      { kind: 'component', text: "Radar chart — the fourth shape in the chart family, and the one the other three could not draw. The donut splits a whole at one instant, the area tracks a single number across time, and the bar compares discrete heights along one baseline; this answers a different question again — what is the overall shape of a set measured on several axes at once — by spoking those measures out from a shared centre and joining them into one silhouette, so balance or lopsidedness reads at a glance rather than value by value. It keeps the family's rules exactly: no plotting library, just SVG polygons and one Framer Motion move, and a single lime, because a radar plots one shape and there is nothing for a second colour to tell apart. The scale is computed, not hand-set — the raw peak is rounded up to a friendly top and divided into round tick values, drawn as concentric facets that share the silhouette's own angles rather than circles floating behind an angular shape, with a small radial scale read up the top spoke so the shape is measured against countable numbers. The reveal is a bloom: the silhouette scales up from the centre point to its full reach, the vertices arriving with it, its transform origin pinned to the chart centre in viewBox units so it grows from the middle and not its own bounding box; under reduced motion it is simply present at full size from the first frame. It is quietly interactive on the family's pattern: every vertex carries a generous transparent hit disc so the whole set is hoverable and, being a real focusable button, keyboard-reachable, and hovering or focusing one lights its axis, rings its point in white, and floats a readout beside it while the rest settle back. Honest to accessibility — the SVG carries a full text summary, every vertex announces its own axis and value, and a plain readout of the whole set sits beneath the picture so the shape is legible without it. It earns its keep on honest data: it draws the shape of the whole site — its pages spoked out across the six sections of the index, read straight off the same contents file the index itself uses — so the Numbers page now shows the site's own silhouette, a small publication with range rather than a single-project landing, replacing the flat by-section bars that stood there. Catalogued in the library under Objects & toys; the component count moves to 145." },
      { kind: 'component', text: "Bar chart — the third piece of data-viz and the one the chart family was missing. The donut splits a whole at one instant and the area tracks a single number moving across time; this answers the plainest question of the three — which of these discrete things is bigger — by standing the categories up as columns so the eye compares their heights directly. It keeps the family's rules exactly: no plotting library, just SVG rects and one Framer Motion move, and a single lime stepped darker by rank so the tallest column reads brightest and colour only ever encodes magnitude, never identity. The axis is computed, not hand-set — the raw peak is rounded up to a friendly top and divided into round tick values (5s, 10s, 25s) so the gridlines land on numbers a reader can actually count by, and the columns are measured against that same top so bar height and gridline agree. The reveal is a grow-up: every column scales from its own baseline to full height, its transform origin pinned to the bottom edge, staggered left-to-right so the bars rise like a small wave being plotted; under reduced motion they are simply present at full height from the first frame. It is quietly interactive on the family's pattern: each column carries a full-band transparent hit target so the whole slot is hoverable and, being a real focusable button, keyboard-reachable, and hovering or focusing one floats a readout above it and caps its top edge in white while the rest dim back. Honest to accessibility — the SVG carries a full text summary, every column announces its own label and value, and a plain readout of the whole set sits beneath the picture so the comparison is legible without it. It earns its keep on honest data: it charts the changelog's own entries by the kind of work each one was — page, component, motion, polish, infra — read straight off the same log the rest of the site counts from, standing the flat by-section breakdown beside it up as columns so the two ways of reading a breakdown sit together on the Numbers page. Catalogued in the library under Objects & toys; the component count moves to 144." },
      { kind: 'component', text: "Area chart — the site's second piece of data-viz and the sibling to the donut, pointed at the other basic question a chart answers: not a part-to-whole spread at one instant, but a single quantity changing across an ordered axis, a trend. Same thesis, no plotting library — just SVG and one Framer Motion move. It holds to a single lime for the same reason the donut does: there is only ever one series, so colour never has to tell two things apart. The lime is the line; a soft lime-to-transparent wash fills the area beneath it so the curve reads as an accumulation and not just a wire. The reveal is a single left-to-right wipe: the line and its fill both point at one animated mask whose width grows from zero, so the whole curve draws itself in as if being plotted, in step, rather than arriving out of sync — and the vertex dots fade in behind the wipe as it passes each one. The curve itself is a Catmull-Rom spline emitted as cubic beziers, so it flows the way a growth curve should instead of reading as a sawtooth. It is quietly interactive: every vertex is a real button, so the whole series is keyboard-reachable, and hovering or focusing one floats a readout above it with that point's label and value while a dashed guide drops to the axis. It earns its keep on honest data — it plots the running total of hand-built components at the close of each chapter of the changelog, read straight from the same log the rest of the site counts from, so it can only ever climb and can never drift from the truth. Honest to accessibility too: the SVG carries a full text summary, each point announces its own value, and a plain readout of every point sits below the picture, so the trend is legible without it. Under reduced motion the mask is full width from the first frame — the finished curve, no draw." },
      { kind: 'component', text: "Donut chart — the site's first piece of data-viz, and the making-by-hand thesis turned on a chart: no plotting library, just SVG arcs and one Framer Motion draw. It is deliberately a magnitude chart, so it holds to a single hue — one lime, stepped darker by rank so the largest slice is brightest and the ring reads lightest-where-largest, never a rainbow of competing colours. Colour is left to carry only how big; identity lives in the legend beside it, where every slice is named with its own swatch, its count, and its share, so no reader ever has to tell two near-limes apart to know what a slice is. Each arc is a plain SVG circle rotated to its start angle and revealed by animating pathLength — the same primitive the site's line-drawing already uses — so the slices sweep themselves in on scroll, one after the next, with a hair of gap between them so neighbours read as separate marks and not one continuous band. It is quietly interactive: hover or focus a legend row and its arc thickens while the rest dim back, and a live figure in the eye of the donut swaps from the running total to that slice's own count and percent. It earns its keep on honest data — it charts the real spread of the component library across its eight families on the Numbers page, read straight off the same library file the rest of the site counts from, so it can never drift from the truth it draws. It de-centres any one project by design: what it shows is the breadth of the hand-built work itself, the whole shelf at a glance rather than a single headline. Honest to accessibility — the ring carries a full text summary for a screen reader while the decorative centre figure is hidden, and the legend is a row of real buttons, each focusable, that drive the same highlight from the keyboard. Under prefers-reduced-motion nothing sweeps: every arc is present at full length from the first frame and the centre figure is set outright. Catalogued in the library under Objects & toys; the component count moves to 142." },
      { kind: 'component', text: "Link preview — a small piece of interface craft, and a turn away from the recent run of generative fields back to a plain, useful micro-interaction: an inline link that floats a rich preview card above itself the moment you hover it or tab to it, so you can feel where a link leads before you follow it. The card is hand-built, not a screenshot service — for a project it shows that project's own generative poster, and for a page it shows a deterministic gradient glyph tile seeded off the title, either way staying in the site's lime-on-ink family. It leans a few pixels toward the cursor as it moves across the link, springed and clamped so it tracks without flying off, and rises in with a soft blur-and-scale that it reverses on the way out. It is deliberately distinct from the site's worded tooltip, which is a small line of text: this is a visual card whose whole point is to pull a reader outward across the breadth of the site from inside a single sentence, so no one project has to carry the prose alone. Honest to accessibility throughout — the link itself stays the real, focusable element that speaks its destination once, while the card is aria-hidden decoration with pointer-events off, so it never intercepts a click and a screen reader never has to wade through it; the preview shows on keyboard focus too, not only on hover, and taps on a touch screen just navigate with no flash of a card. Under prefers-reduced-motion the cursor tracking, the blur, and the scale all drop for a plain, instant appearance. It debuts as a full-width Type & text experiment in the Playground and lands for real in the home page's Range intro, where the words Guided and across disciplines each come forward into a preview — Guided framed in its own card as one project, not the whole story, and the range as five disciplines laid out. Catalogued in the library under Overlays; the component count moves to 141." },
      { kind: 'component', text: "Game of Life — Conway's Game of Life, rebuilt as a phosphor pointer field, and a genuinely new kind for the family: where the Ripple tank and Pendulum wave beside it are continuous physics, this is the most famous discrete system there is. Every cell on a grid lives or dies each generation by four rules that fit on one line — a live cell with two or three live neighbours survives, a dead cell with exactly three is born, everything else dies — and out of nothing but those rules come gliders that walk, blinkers that tick, and drifting soup that never settles the same way twice. The board is a torus: its four edges wrap, so a glider that walks off the right side reappears on the left and travels forever, and nothing piles up against a wall. It is fully live under the pointer — drag and you paint living cells straight into the soup along the whole path between frames so a fast swipe never dots, press and you stamp a bright cluster and watch what it grows into; left alone the board keeps itself alive, dropping a seeded blob of soup at a wandering point whenever the population thins and launching the odd glider so something is always travelling. The look is a phosphor screen, not a checkerboard: alongside the honest 0/1 automaton runs a second heat field that pins to full where a cell is alive and bleeds off over a few generations where one just died, so the board glows lime where life is and leaves cooling ghosts where it just was — painted to a grid-sized offscreen buffer at one pixel per cell and smoothed up to full width by the GPU, so live cells bloom into soft points of light and the hot loop touches about ten thousand cells rather than a million pixels. No Date.now and no Math.random on the hot path — the opening soup and every idle drop are placed by a seeded PRNG and the generation timing is off the frame counter — so it is stable across resizes and never trips the environment's guards. It debuts as a full-width Pointer fields experiment in the Playground, right after the Ripple tank, so the continuous surface and the discrete grid read against each other. Under prefers-reduced-motion the loop never starts: a seeded board is stepped a few dozen generations until the soup resolves into still-lifes and oscillators, then painted in a single still frame. Catalogued in the library under Pointer fields; the component count moves to 140." },
      { kind: 'component', text: "Bottom sheet — the native iOS sheet, rebuilt for the web, and a deliberate turn away from the run of generative physics toys toward a real product control. It is the pattern behind Apple's UISheetPresentationController: a panel anchored to the bottom of the screen that rests at one of several heights — its detents — rather than being simply open or closed. Drag the grabber at its top and it follows your finger, then on release snaps to the nearest of three stops (peek, half, full), throwing further when you flick it by projecting the release velocity forward before it picks a stop; pull it down past the smallest stop and it dismisses. A backdrop dims in exact step with how far the sheet is raised, driven off the same motion value as the translate, so the room behind darkens as the sheet climbs. It is a separate component from the site's plain Modal on purpose — Modal is a single card that is either shown or hidden, whereas here the height change, the detent snapping, and the drag-to-dismiss are the whole control, and they need their own motion model. The drag is bound only to the grab region, so the content below scrolls independently the way a real sheet's does. Honest to accessibility throughout: role=dialog with aria-modal, Escape to close, focus moved into the panel on open and restored to the trigger on close, a Tab trap that keeps focus inside, body-scroll lock while it is up, and a labelled grabber that doubles as a button cycling the detents from the keyboard alongside an always-present peek/half/full segmented control — so every height is reachable without a drag. Under prefers-reduced-motion the drag comes off entirely and the same three stops move the sheet instantly, with no throw and no spring. It reinforces the native-iOS side of the work with something you can actually operate, not just a claim, and debuts as a full-width controls-band showcase in the Playground beside the Dynamic island. Catalogued in the library under Navigation & controls; the component count moves to 139." },
      { kind: 'component', text: "Ripple tank — the Pendulum wave taken into two dimensions, and a full-surface counterpart to it in the pointer-field family. Where the Pendulum wave beside it is a single row of oscillators timed into a travelling wave, this is the two-dimensional case: a real shallow-water surface where a disturbance spreads as a widening ring, meets the tank walls, reflects, and interferes with every other ring in flight. The physics is the honest 2D wave equation discretised with the five-point Laplacian — next = 2·cur − prev + c²·(Σ neighbours − 4·cur) — leapfrogged across two height buffers that swap each frame, with c² held at 0.28, safely under the 0.5 stability ceiling for that stencil, and a hair of damping per step so old rings fade and the tank settles between drops. The edges are held fixed, so waves bounce off the walls the way they do in a real ripple tank, and that reflection is where the standing interference lattice comes from — the same reason a tank is the classic classroom demo for wave superposition. It is fully live under the pointer: drag and you draw a continuous wake (drops laid down the whole segment between frames so a fast drag never dots), press and you drop a stone; left alone, a seeded drip keeps the surface breathing on a slow interval so it is never dead. The height field is turned into lime caustics by shading each cell against its own slope — a fake grazing light standing in for the surface normal, so crests tip toward white and troughs fall dark — painted once to a grid-sized offscreen buffer and let the GPU smooth it up to full width, so the hot loop touches about ten thousand cells rather than a million pixels. No Date.now and no Math.random on the hot path — the idle drips are timed off the frame counter and placed by a seeded PRNG — so it is stable across resizes and never trips the environment's guards. It debuts as a full-width Pointer fields experiment closing that band in the Playground, right after the Pendulum wave, so the one-dimensional rack and the two-dimensional surface read against each other. Under prefers-reduced-motion the loop never starts: a few fixed drops are stepped to a settled interference lattice and painted in a single still frame. Catalogued in the library under Pointer fields; the component count moves to 138." },
      { kind: 'component', text: "Pendulum wave — a generative field grown from the plainest physics there is, and a new kind again for the pointer-field family. Where the Attractor beside it grows from deterministic chaos and the Murmuration from a flock, this one is just a fanned rack of simple pendulums hung in a row, longest to shortest. The whole thing lives in the tuning: over one shared cycle the i-th pendulum completes exactly one more full swing than its neighbour — whole numbers all — so after that cycle every bob returns to the same place at the same instant and the rack snaps back into a single clean line. In between, because no two rates match, the bobs drift out of step and the eye reads the drift as a wave travelling along the rack: first a smooth snake, then a split into two and three waves, then a scatter that looks random but never is, then a clean re-alignment as the counts come due. It is the Harvard lecture-hall demo, drawn in lime light. Each angle is a pure cosine of a frame-driven time phase, θ = A·cos(ω·t) — no springs, no integration drift, no randomness, no wall clock — so it is stable across resizes and never trips the environment's guards. The string lengths fan by 1/ω² the way a real pendulum's period does, so the slow low notes hang long and the quick ones race, and each bob brightens at its turning points where it hangs an instant. The rack is live: press anywhere and the phase eases back to zero, lifting every bob to the same side so the wave re-forms from a clean line under your hand rather than jumping. It debuts as a full-width Pointer fields experiment in the Playground, closing that band beside the Attractor, so chaos and clockwork read against each other. Under prefers-reduced-motion the loop never starts — a single frame is painted at a phase that shows one graceful wave, at rest. Catalogued in the library under Pointer fields; the component count moves to 137." },
      { kind: 'component', text: "Attractor — a generative field grown from deterministic chaos, and a new kind again for the pointer-field family. Every other field there grows from something physical or geometric: the Phyllotaxis packs points by the golden angle, the Morphogen reacts two chemicals, the Murmuration steers a hundred birds, the Contour surveys drifting hills, the Truchet re-threads a weave. This one grows from a single idea — iterate one simple nonlinear map over and over and watch where its orbit lands. The map is the Clifford attractor: x' = sin(a·y) + c·cos(a·x), y' = sin(b·x) + d·cos(b·y). Feed a point in, get a point out, feed that back in, forever. Any single step is unremarkable, but the orbit never settles and never repeats — it wanders a bounded fractal set of infinite detail, folding the plane over on itself so densely that where the orbit lingers reads as light and where it rarely visits stays dark. That is the whole idea of a strange attractor: a bounded region a deterministic rule fills without ever closing into a loop. It is drawn by plotting thousands of orbit steps a frame with additive compositing, so overlapping visits sum into real brightness — dense arms bloom from lime toward white while the faint rim stays cool, a density map rather than a line. The four parameters a, b, c, d are what pick which attractor you get, and small changes redraw the whole shape, so the pointer morphs the family — the cursor's x leans a and its y leans b — while an idle drift keeps all four breathing on slow, out-of-phase sines, so the shape endlessly re-forms whether or not you touch it. The orbit starts from a fixed seed and the first steps of transient are dropped each frame; there is no randomness anywhere, the drift is an eased time phase and not an RNG, so it is stable across resizes and never trips the environment's guards. One canvas, one loop, no per-point React state, DPR-capped and ResizeObserver-driven. It debuts as a full-width Pointer fields experiment closing that band in the Playground, beside the Contour, so the surveyed terrain and the chaotic orbit read against each other. Under prefers-reduced-motion the loop never starts: a single, crisper attractor is plotted once and held still, no pointer." },
      { kind: 'polish', text: "The home page's work band was the last flat corner of the site — a plain two-column grid of text cards while the /work ledger and every band around it had real craft. It is now a poster-forward showcase: each real project is a large card carrying its own generative poster, and the whole card tilts in 3D toward the cursor under a lime spotlight while the poster lifts and a lime edge wipes down its side. The same treatment lands on every project equally — Guided and this site get the identical poster card — so no single one is framed as the whole story, and the placeholder for future work reads as a calm dashed invitation across the row rather than a dead tile. Both honest ways in are kept: the poster and a pill open the quick-look modal, the title and a link go through to the full case study. It is a new reusable component (ProjectShowcase), catalogued in the library under Cards & surfaces, and it is reduced-motion aware — the tilt and the spotlight come off and the cards simply reveal in place." },
      { kind: 'page', text: "Principles — a manifesto page for how I actually build. Eight beliefs, ordered the way the work really moves: start with the problem, prototype in the real thing, own the whole stack, make motion earn its place, sweat the small moments, make it rather than assemble it, ship then refine in the open, and stay honest. This is the belief layer the rest of the site only implies — About tells it as story, the home Ethos says it in a single line, the Craft notes demonstrate it in motion; here it is set down plainly and read on its own. Every principle is one already lived out elsewhere, so each links to where it holds up in the real work — the Playground for prototyping in code, the Range for the whole stack, By the numbers for made-not-assembled, the changelog for shipping in the open. The reading column is flanked on wide screens by a sticky spine that tracks your place through the eight, an IntersectionObserver lighting the current belief lime; a huge ghost numeral sits behind each credo, the credo itself splits in on view, and the honest body assembles word by word under the scroll. Nothing here is claimed that the site does not already back — a manifesto, not a wish list. Reduced motion drops the splits and the scroll assembly for finished, still text. Wired into the index (under Words), the command palette, the g-v go-chord, the footer, and the terminal; the page count moves to 26." },
    ],
  },
  {
    marker: '05',
    title: 'Toys that light up',
    summary:
      'A run of interactive, cursor-aware experiments — the kind of thing the playground exists to hold. Each one is its own hand-built component, wired into the pages that suit it.',
    items: [
      { kind: 'component', text: "Abacus — a working soroban, and the odd one out in the toys family. The Clock is a passive dial, the SplitFlap clatters between fixed strings, the Turntable spins with momentum, the Harmonograph plots a seeded figure — every one of them is motion for its own sake. This one holds state that actually means something: an object you reckon on. Each rod carries one heaven bead worth five above the reckoning bar and four earth beads worth one below it, and a bead counts once it is pushed toward the bar, so a rod reads zero to nine and the columns left to right are the decimal places, most significant first. The big number above the frame is the honest sum of every bead's place — move a bead and the arithmetic follows, nothing about the readout is decorative. Clicking the kth earth bead carries the beads it should carry with it, the classic soroban toggle: tap a bead that is up and it and everything below it drop; tap one that is down and it and everything above it rise. It is not click-only either — every rod is a real spinbutton that reads its live digit to a screen reader, and focusing one lets you drive its value straight from the arrow keys, Home and End, or a number key. The unit-rod dot sits on the ones column the way it does on a real instrument. It debuts as a full-width Objects & toys experiment, seeded to the year. Under prefers-reduced-motion the bead springs come off and they snap into place — still a fully usable, fully labelled soroban, just without the travel." },
      { kind: 'component', text: "Cubes — the pointer-field family's first surface with real height. The dots (DotGrid), needles (MagnetLines), lattice (Squares) and canvas simulations were all flat; this one is volumetric — a grid of little blocks laid on an isometric ground plane, each built from a top face and two lit walls in pure CSS 3D, no canvas and no WebGL. The block your cursor is over lifts and its top face lights lime, and a ripple radiates outward: nearer blocks rise first and highest, farther ones a beat later and less, via a per-cube transition-delay keyed to grid distance, so the raise reads as a wave leaving your finger rather than every block snapping at once. It is index-driven, not coordinate math — each cube reports itself on pointerenter, so the active cell only changes when you cross into a new block and every other cube derives its raise and delay from its distance to that one; no per-pixel state on the hot path. The whole plane keeps a gentle idle shimmer, the top faces breathing on a staggered diagonal loop, so the field is alive before you touch it. It debuts as a full-width Pointer fields experiment. Under prefers-reduced-motion the shimmer, the ripple transitions and the entrance all fall away and the plane sits flat and still." },
      { kind: 'component', text: "Phyllotaxis — a living sunflower head, and a generative field that grows out of geometry alone where the Morphogen next to it grows out of chemistry and the Murmuration out of a hundred steering birds. This one pushes no mass and reacts no chemicals; it places nothing but points and lets a single irrational number do all the work. It is the packing a sunflower, a pinecone, or a daisy uses to fit the most seeds into a disc with no gaps and no seams — Vogel's 1979 model, where seed i sits at angle i times the golden angle (360 degrees divided by the golden ratio squared, about 137.5 degrees) and radius proportional to the square root of i. That one angle is the entire trick. Because the golden ratio is the most irrational number — its continued fraction is all ones, so it resists any rational approximation longer than any other value — consecutive seeds never line up into spokes, and the florets pack evenly all the way out to the rim; the eye then reads the packing as two interleaved families of spirals, the parastichies, whose counts are always adjacent Fibonacci numbers (21 one way, 34 the other, and on up). To make the geometry legible rather than a static poster, the divergence angle breathes a few thousandths of a radian around the golden angle: nudge it off and the spiral arms visibly wind, unwind, and snap to a new Fibonacci family, which is exactly the demonstration that the golden angle is special — only there do the seeds stop clumping into arms at all. The whole head turns slowly, the florets ripen from cool white at the core toward lime at the rim, and the pointer is a lens that swells and warms the florets nearest the cursor, brightest at the centre and falling off with distance. One canvas, one animation loop, the seed index, angle, and radius pure math with no per-floret state and no randomness — the shimmer comes from an eased time phase, not an RNG, so it is stable across resizes and never trips the environment's guards. It debuts as a full-width Growth experiment beside the Morphogen, so the geometric packing and the chemical pattern read against each other. Under prefers-reduced-motion the loop never starts: the head is drawn once at the exact golden angle, fully grown and still." },
      { kind: 'component', text: "Morphogen — a living Turing pattern, and a field grown by chemistry where every other simulation on the site pushes mass around. The Cloth's Verlet nodes, the Murmuration's boids, the Ballpit's spheres, the Truchet's tiles — all of them move bodies through forces. This one moves nothing; it grows a pattern out of a pair of reacting, diffusing chemicals and nothing else. It is the Gray-Scott reaction-diffusion system, the modern form of the mechanism Alan Turing set out in his 1952 paper 'The Chemical Basis of Morphogenesis' to explain how a featureless egg decides where a leopard's spots or a fish's stripes go. Two virtual chemicals share the grid: A, everywhere at rest, and B, the catalyst. Wherever they meet B consumes A and makes more of itself, A is fed back in at a steady rate, and B slowly decays — and crucially both spread by diffusion, a weighted 3x3 Laplacian, but A spreads twice as fast as B. That single imbalance is the whole trick: left to run, the field can no longer stay uniform and self-organises into coral, worms, and mitosing cells, a stable pattern that never quite settles. It runs on a downscaled float grid wrapped toroidally so the pattern is seamless at the edges, several reaction steps a frame, double-buffered in flat Float32Arrays with no per-cell React state, and drawn once into a small ImageData that the browser scales up — which is what gives the soft biological edges for free. The pointer is a pipette: it injects catalyst in a soft disc under the cursor, so fresh growth blooms in your wake and then heals back into the field. The seed blobs and the pointer dose are deterministic — hashed, never random — so the pattern is stable across resizes. It debuts as a full-width Chemistry experiment beside the Truchet, so the chemical field and the geometric weave read against each other. Under prefers-reduced-motion the loop never starts: the reaction is run forward once to a mature pattern and drawn a single time, no pointer, just the finished morphogenesis." },
      { kind: 'page', text: "Atlas (/atlas) — the whole site drawn as a living constellation, and the most structural way yet of saying the thing the site keeps saying in words: one project is a single star among many. Where the index lists the pages and the library catalogues the components, this shows the shape of the site — a real force-directed graph where a central node anchors the middle, each section is a hub on a ring, and each page is a star orbiting its hub. It is honest physics, not a decoration bolted onto one: every node repels every other by an inverse-square law, springs pull the edges toward a rest length, a weak gravity keeps the whole thing centred, and a tiny per-node ambient sway keeps the sky breathing after it settles. Grab any star and drag it and the rest re-solve around it, reheating the simulation, then ease back when you let go; hover a section and its whole cluster lights up in lime with the page names revealed while the rest of the sky dims away. It is built from the same contents source the index reads, so the two can never drift, and Guided sits on it as one leaf under Work — not the centre. No React state on the physics hot path: positions live in refs and the loop writes transforms straight onto the nodes and coordinates onto the edge lines, so only the hover highlight ever re-renders. Under prefers-reduced-motion the solver runs headless to a settled layout and the map is drawn once and held still — hover still lights a cluster, and dragging is disabled. Reachable from the command palette, the g-k chord, the index, the terminal, and the footer." },
      { kind: 'component', text: "Word clock — the third way this site reads its Berlin time, after the drawn analog dial and the rolling split-flap digits. This one is pure type: a fixed 11-by-10 wall of letters, laid out in the classic QLOCKTWO English arrangement, where only the words that spell the current time light up — IT IS HALF PAST TEN read out of an otherwise dim field of glyphs. The grid is hand-laid so every word it needs sits at a known coordinate and every remaining cell is still a real letter, no blanks, with a couple of filler letters chosen so the rows stay full; a small verification confirmed each word lands exactly where the reader expects it. Time is read to the nearest five minutes — the convention these clocks have always used — from a real timezone via Intl, so it is honest and DST-safe, and it rolls the hour forward correctly for the 'twenty-five to' half of the dial. Only the set of lit cells drives React state, and although it ticks every second it compares before it commits, so a re-render happens just four times an hour when the phrase actually turns over, not on every tick. It debuts as a surfaces experiment in the playground beside the analog Clock, giving the site's small family of time pieces a typographic third member. Reduced motion keeps the reading correct but snaps the glow in rather than breathing it, and the grid carries an aria-label and a polite live region that speak the time in plain words." },
      { kind: 'component', text: "Truchet — a living weave of arc tiles, and a pointer field of a wholly different kind from the physics that came before it: no bodies, no forces, just pattern re-threading itself. It rebuilds the oldest trick in generative art, from Sebastien Truchet's 1704 study of a single divided square. Every cell of the grid draws one of two things — a pair of quarter-circle arcs joining its top edge to its left and its bottom to its right, or the mirror of that joining top to right and bottom to left — and because the arcs always meet the edges at the same four midpoints, tiles laid edge to edge join into long flowing curves, loops, and knots regardless of how they are turned. What makes this one alive is that the orientation is not fixed: a slow scalar field of three drifting sine waves decides each tile's turn every frame, so the maze re-threads itself as the waves pass, each tile flipping on its own beat rather than in lockstep, and every flip cross-fades from the old arcs to the new — held in a per-tile progress array — so the pattern breathes instead of snapping, with a dead-band around the zero crossing so a tile hovering at the threshold never flaps. The pointer is a rotating four-lobe pinwheel pressed into the field: tiles near the cursor bend toward it and warm from cool white to lime, brightest at the centre and falling off with distance, so a lit domain curls along behind the cursor. One canvas, one animation loop, the current orientation, the previous orientation, and the flip progress all held in flat typed arrays with no per-tile React state, DPR-aware and ResizeObserver-driven. It debuts as a full-width Pointer fields experiment beside the Cloth, so the generative pattern and the connected soft body read against each other. Under prefers-reduced-motion the loop never starts and there is no pointer: the field is frozen at time zero and the weave is drawn once into a single calm, balanced pattern." },
      { kind: 'component', text: "Cloth — a fourth take on physics, and the odd one out of the set. Gravity's boxes, the Ballpit's spheres, and the Murmuration's birds are each a cloud of separate bodies bumping into one another; this is a single connected soft body. A grid of point masses is integrated with Verlet — each point remembers only where it was last frame, and velocity is read as the gap between then and now, no stored velocity vector at all — and the whole sheet is held together by distance constraints relaxed over several passes every frame, the classic recipe for cloth. Fabric resists a stretch far more than a fold, so the solver pulls an over-stretched link back hard and lets a compressed one barely push, which is what makes the sheet crease into soft folds instead of snapping taut like a trampoline. The top edge is pinned in scallops so it drapes into valances like hung bunting, a slow two-frequency breeze keeps it breathing with no pointer, and dragging through it catches the weave and shoves it open. Each cell is shaded by how much it compresses horizontally — dark in the folds, bright where it stretches flat — over a faint thread weave, so the folds catch an overhead light. One canvas, one loop, the nodes in a flat ref array with no React state on the hot path, a fixed physics substep so the constraint solve stays stable at any frame rate, DPR-aware and ResizeObserver-driven. It debuts as a full-width Objects & toys experiment beside the Ballpit, so the connected soft body and the loose spheres read against each other. Under prefers-reduced-motion the loop never starts: the solver runs headless to relax the cloth into one settled drape, drawn once." },
      { kind: 'page', text: "The range (/range) — a whole page for the thing the site most needed said out loud: that Guided is one project, not the shape of the work. It names the five disciplines the Home Range band only glances past — Frontend, Native iOS, Backend and data, Applied AI, Motion and design — and gives each one room, headed 'More than one project.' A keyboard-navigable tablist down the left (arrow keys wrap the selection, a springing lime marker rides the active row) drives a spotlight panel that cross-fades between disciplines, each with an honest paragraph, an in-practice list where every line is something actually done rather than a metric, the real tools reached for, and a link to existing evidence — the Playground, the component library, the Guided case study, the toolkit. It closes on a 'five disciplines, one craft' counter and a plainly stated preference: to be a builder who can carry an idea from a data model to the last frame of an animation rather than hand it off four times. The content lives in one honest source, src/data/disciplines.ts, so the Home band and the page can never drift. Reduced-motion aware throughout, lazy-loaded into its own chunk, and reachable from an 'Explore the range' link on the home page, the command palette, the g x chord, and the index." },
      { kind: 'component', text: "Murmuration — a flock of darts that wheels, streams, and folds in on itself like starlings at dusk, and a pointer field unlike any other on the page. Every other field here is authored and the cursor only bends it: DotGrid springs the nearest dots, FlowField advects motes down a frozen noise field, Threads bulges set waves, MetaBalls fuse blobs. This one has no field at all. Each of a hundred boids steers only by the three classic Reynolds rules over the neighbours it can actually see — separation (keep your distance), alignment (match their heading), cohesion (drift toward their centre) — and the sweeping, self-composing shape of the whole flock is nobody's design; it emerges from a hundred agents each minding a small neighbourhood. The cursor is a bird among them: hover and the flock leans toward you, press and you are a hawk and they scatter. One canvas, one animation loop, the whole flock in flat typed arrays with no React state on the hot path; every boid's steering is read from the previous frame's positions before any of them moves, so the flock turns in lockstep rather than in array order. Trails are eroded toward transparent with a destination-out wash so the panel's own gradient shows through and dense knots smear into light, the step is clamped so a backgrounded tab can never explode the sim, and the scatter is seeded so the opening is lively but identical each mount. It debuts as a full-width Pointer fields experiment right beside the authored Flow field, so the emergent flock and the designed current read against each other. Under prefers-reduced-motion the loop never starts: a fixed number of silent steps settle the flock into one composed, still murmuration, drawn once." },
      { kind: 'component', text: "Ballpit — a second take on physics, and a deliberately different one from the Gravity bin next to it. That one drops DOM pills and resolves them as axis-aligned boxes; this drops round bodies onto a canvas and resolves real circle-circle contact — each pair separated along the line between their centres and given an impulse along it, with the bigger ball (mass scales with its area) shoving the smaller one more than the reverse. The balls are drawn as lit spheres — a radial gradient for volume, a specular dot, a soft cast shadow — so it reads as a physical thing rather than a tag cloud. One canvas, one animation loop, the whole simulation living in refs with no React state on the hot path, DPR-aware and ResizeObserver-driven, the step clamped so a backgrounded tab can never explode it; grab a ball and the motion you impart is carried into the toss. It debuts as a full-width Objects & toys experiment in the Playground, alongside the Gravity bin so the two crafts — contact between rectangles, contact between circles — read against each other. Under prefers-reduced-motion the loop never starts: the balls settle into a calm, static pile instead." },
      { kind: 'component', text: "PlaygroundFinder — a scoped command-palette for the Playground, which had grown to three dozen experiments down one very long scroll with no way to reach one by name. Press the floating 'Find an experiment' pill or the / key and it reads every experiment card straight off the live page — so it can never drift from what is actually rendered — then lets you search by name, arrow through the results grouped by category, and jump. Enter or a click scrolls the card to the middle of the viewport and blooms a brief lime ring around it so the landing is unmistakable, a 'Surprise me' throws you at one at random, and every experiment now carries a copy-able deep link (a Link pill on hover) that scrolls and flashes it on load — so a specific experiment is finally shareable by URL. The jump is deferred until the overlay has fully closed, so the scroll never fights the exit animation. Under prefers-reduced-motion the ring holds as a plain static outline and the scroll is instant." },
      { kind: 'polish', text: "Fixed a real crash the finder surfaced: ClickSpark, the site-wide lime burst under every click, could throw an IndexSizeError from a negative arc radius. A burst spawned inside a pointerdown handler stamps its birth time with performance.now(), but the animation-frame callback that draws it is handed the frame's start timestamp — under load the two can invert, driving the ring's eased radius below zero and breaking that frame's whole animation loop (which is what stalled the finder's close). The ring and spark life are now clamped so the eased value can never run past its ends." },
      { kind: 'component', text: "Contour — a living topographic map, and a pointer field of yet another kind. Where every other field on the site reacts locally at the cursor — DotGrid springs the nearest dots, MagnetLines turns needles, Threads bulges its waves, MetaBalls fuse blobs, PixelTrail ignites a grid — and even the Kaleidoscope only folds the pointer's own path, this one is a whole terrain. A smooth elevation field of a handful of soft Gaussian hills, each drifting on its own slow lissajous path, is resampled onto a grid every frame and then traced into iso-lines with marching squares — the exact contour a real map uses to draw elevation, linear-interpolated along each cell edge with both saddle cases drawn so the rings never break. The honest read of height is in the colour: each ring is warmed from cool white toward lime in proportion to the level it sits at and thickened a touch, so peaks glow and the lowlands stay faint. The pointer is a hill of its own — an elevation bump that eases under the cursor and swells in while it hovers, then fades when it leaves — so a bright lime bullseye rises wherever you rest and the surrounding land bends around it, and where it meets a drifting hill the rings merge like the saddle between two peaks. One canvas, one animation loop, a single Float32 field resampled per frame with no per-line React state, DPR-capped and ResizeObserver-driven. It debuts as a full-width Pointer fields experiment closing that band in the Playground. Under prefers-reduced-motion there is no loop and no pointer — the field is frozen at time zero and contoured once into a calm, balanced relief." },
      { kind: 'component', text: "Kaleidoscope — a living mandala painted by the pointer's own motion, and a pointer field of a different kind from every other one on the site. The rest of the field family all react locally at the cursor: DotGrid springs the nearest dots, MagnetLines turns needles, Threads bulges its fixed waves, Ribbons chase the pointer, MetaBalls fuse blobs, PixelTrail ignites a grid — this one does not nudge a field at all. It takes the path the pointer traces and reflects it into six-fold rotational and mirror symmetry, then lets the strokes accumulate into a slowly-fading, self-composing figure. The honest mechanic is a decay-and-build: a translucent fill of the background colour dims the whole surface every frame — that is the trail — while the fresh strokes composite additively so crossings build into light, and the hue drifts along an on-brand lime-to-teal-to-sky ramp so the figure stays warm and lime-led rather than a rainbow. Move slowly and you draw deliberate petals; sweep fast and you fling bright arms out to the rim; leave it alone and a gentle lissajous keeps it blooming on its own, so the field is never empty. One canvas, one animation loop, no per-point state on the hot path, DPR-capped and ResizeObserver-driven. It debuts as a full-width Pointer fields experiment closing that band in the Playground. Under prefers-reduced-motion it lays down a single still, seeded mandala once — no loop, no listeners." },
      { kind: 'page', text: "Type specimen (/specimen) — the type this site is set in, up close and interactive. Its centrepiece drives the two real axes of the self-hosted variable Fraunces by hand: a Weight slider (300–700) and an Optical size slider (9–144, the axis the rest of the site tracks automatically), both writing straight to font-variation-settings on a live headline so you watch the letterforms redraw as you push them, with the resulting CSS shown and copyable. Below it, a switcher sets a handful of honest sample lines or your own words, the seven-step type scale renders live in rem exactly as the pages step through it, the full character set is laid out at display contrast with a hover lift, and a pairing block shows Fraunces and Inter doing their two jobs — voice and clarity — side by side. It sits apart from the Design language page, which catalogues type as static tokens; this one puts the variable font in your hands. Reduced-motion drops the axis transition and the entrance moves; the sliders stay fully usable." },
      { kind: 'component', text: "Carousel — a stories-style panel sequence, the auto-advancing 'tap through' pattern the library never had. It sits deliberately apart from its nearest neighbours: CircularGallery fans cards into a draggable 3D coverflow you scrub by hand, CardStack quietly cycles a passive 3D deck on a timer, and InfiniteScroll loops a column that never re-orders — this one runs a timed sequence of full panels with a row of segmented progress bars filling across the top, the way an Instagram or stories viewer does. The honest mechanic is that the bar is the clock: the active segment fills over the interval and, the instant it tops out, the panel advances — so the progress you see and the timing that drives it are the same thing and can never disagree, and pausing freezes both at once. It is a sequence you can drive rather than only watch: hold anywhere to pause, click the left or right edge to step, swipe on touch, tap a bar to jump straight to a panel, or use the arrow buttons and the keyboard. Honest to assistive tech — the controls are real labelled buttons, a polite live region names the current panel, and the fill is written straight onto the DOM each frame with no React state on the hot path. It debuts as a full-width Navigation & controls experiment carrying honest frames about how the work is made, and it takes a real place on the About page as 'The short version' — the intro cut into glanceable frames that name Guided as one thing among several rather than the whole story. Under prefers-reduced-motion the auto-run and the fill drop away entirely and it becomes a plain, manually-stepped sequence, fully legible and fully controllable." },
      { kind: 'component', text: "Sortable — a drag-to-reorder list, the first control in the library where the state itself is the toy. It sits deliberately apart from its neighbours in the controls family: AnimatedList springs a highlight between fixed rows, InfiniteScroll loops a column that never re-orders, and Accordion opens one row at a time — here you rearrange the rows themselves and the layout re-solves live underneath. The trick is that every row is laid out absolutely by its index, so the whole reorder collapses to one number — index times the row pitch — and there is nothing to measure: the dragged row simply tracks the pointer while every other row springs to its new slot, and the instant the dragged row's centre crosses a neighbour the order re-solves and the rest slide to make room. Let go and it settles into the gap it made. Honest to the keyboard and to assistive tech, not drag-only: each row is a real reorder control with an aria-label, ArrowUp/ArrowDown move it one slot and Home/End send it to the ends, and a polite live region reads the new position aloud — so it reorders exactly the same by keyboard as by hand. It debuts as a full-width Navigation & controls experiment carrying a demo order — a way of working, Understand → Sketch → Prototype → Build → Refine → Ship — that is a way of working rather than a claim, there to be rearranged. Under prefers-reduced-motion the spring settle comes off and rows jump straight to their slots, but the list reorders identically by drag or by arrow key." },
      { kind: 'component', text: "Harmonograph — the Victorian pendulum drawing machine, rebuilt in maths. Two or three pendulums, one swinging a pen and the others swinging the paper, trace the interference of their motions as they wind down; here each axis is the honest sum of two decaying sinusoids — x(t) = Σ aᵢ·sin(fᵢt + pᵢ)·e^(−dᵢt) — and it is the near-integer frequency ratios with a hair of detuning that make the loops precess into a figure instead of closing on themselves. It sits deliberately apart from the rest of the Objects & toys family: Clock is a passive dial, SplitFlap clatters between fixed strings, the Lanyard is a weight on a string, and the Turntable is a wheel with momentum — this is a deterministic plotter, and the same seed always draws the same plate. The whole curve is pre-sampled once from a seedable PRNG (mulberry32, no per-frame entropy) into a flat Float32Array, then a pen tip replays it in real time; crossings glow because the trace is composited additively ('lighter'), so density reads as light the way graphite builds on paper. The canvas is DPR-aware and ResizeObserver-driven with no React state on the hot path, and it is aria-hidden decoration with an sr-only account beside a real 'New figure' button. It debuts as a full-width Objects & toys experiment in the Playground; click the plate or the button to hang a fresh set of pendulums. Under prefers-reduced-motion the finished figure is laid down in one frame — no travelling pen, the same seeded plate at rest." },
      { kind: 'component', text: "Turntable — a record deck you can actually spin, the first object in the library with free momentum. It sits deliberately apart from its neighbours in the Objects & toys family: Clock is a passive dial reading a real timezone, SplitFlap clatters between fixed strings, and the Lanyard is a single weight swinging on a string — this is a wheel you impart angular velocity to and watch coast down. The platter carries one number, an angular velocity in degrees per second, integrated every frame off the animation clock with no React state on the hot path. Play eases it up to 33 rpm and springs the tonearm down onto the lead-in groove; grab the vinyl and the rotation is driven straight from the pointer angle, and the hand motion you impart becomes the velocity it keeps when you let go, bleeding off on friction until it stops. Honest to the machine and to assistive tech: the deck is a real play/stop button carrying aria-pressed, the platter and arm are aria-hidden decoration, and the physics is genuine — a flick really does coast longer than a nudge. It debuts as a full-width Objects & toys experiment in the Playground, its label reading 'Side A / hand-built in Berlin' — a small personal beat rather than anything about a client. Under prefers-reduced-motion nothing spins: the record sits still with the tonearm resting on it and a plain, readable label." },
      { kind: 'component', text: "Lamp — an overhead light rig that switches on as a statement scrolls into view, in the spirit of Aceternity's Lamp and rebuilt here from scratch. It sits deliberately apart from the site's other light effects, which all move a light around: SpotlightReveal is a torch you drag across hidden words, ChromaGrid dims everything but a hole under the cursor, and Aurora, Iridescence, and Lightning are ambient fields — this is a fixed overhead lamp that ignites once when the section arrives. A thin glowing tube stretches from a hairline out to full width, a beam fans down beneath it, a soft pool of light blooms, and the heading rises up into the light the rig casts — the ignition sequence is the reveal. The honest part is that the whole rig is additive: semi-transparent lime, heavily blurred, so it lights whatever sits behind it — the Aurora, a page's own background — rather than painting a dark box over it the way a masked-cone lamp would. No canvas and no RAF loop, just three Framer Motion layers keyed to a once-only whileInView, and the light is aria-hidden so the words carry all the meaning. It lights the closing statement on the work page — 'Have something worth building?' switches on as you reach the bottom of the ledger, a closing beat that belongs to the site rather than to any one project. It debuts as a full-width Type & text scene in the Playground, replayable so you can flick it off and on and watch it ignite. Under prefers-reduced-motion every layer renders already switched on and the heading sits still in the pool — the composition is the point, the ignition is the flourish." },
      { kind: 'component', text: "CodeInput — a segmented one-time-code / passcode field, the entry control the controls family never had. It sits deliberately apart from its neighbours, which are all single controls: ElasticSlider is a continuous range, Knob a rotary dial, Switch a binary toggle, Stepper a discrete count — none is a multi-cell field you fill character by character. This is where the library's give-it-real-physics language meets the unglamorous product-engineering craft the pattern actually demands: focus that hands itself forward as you type and back as you delete, a paste that scatters its characters across the empty cells, full keyboard travel (arrows, Home/End, Backspace/Delete), and a per-cell real <input> a screen reader can name and drive. The feel is in the detail: each landing character springs in — scaling and lifting out of nothing rather than blinking on; the focused cell wears a lime ring and a steps() blinking caret while it is empty; and the moment every cell fills the whole row lifts in a short, once-only lime-lit stagger to mark completion. onComplete fires exactly on the transition into a full code, not on every keystroke while it stays full. Controlled or uncontrolled, numeric or alphanumeric, and autofill-ready — autocomplete=one-time-code and inputMode=numeric on the first cell so an SMS code drops straight in. It debuts as a Navigation & controls experiment: a live six-cell field wired to a real success state — filling it flips the field to a 'Code entered' chip and raises a toast, with a Reset that clears it back to empty. Under prefers-reduced-motion the pop, the caret blink, and the completion lift all come off and it stays a plain, legible field that still reads and responds to assistive tech." },
      { kind: 'component', text: "Lens — a magnifying loupe that follows the pointer across a surface and reveals a zoomed circle of it in place, in the spirit of Aceternity's Lens. It sits deliberately apart from its nearest neighbours: SpotlightReveal uncovers hidden copy under a moving mask, Crosshair lays measuring hairlines over a surface, and Lightbox opens a piece of media fullscreen — this one magnifies what is already there, without moving it. The mechanic is one radial mask, not a second scaled clone chased into alignment: the children are painted twice — once normally, once as an aria-hidden overlay scaled up with its transform-origin pinned to the cursor — and a radial-gradient maskImage shows only a circle of the magnified layer around the pointer. Pinning the origin to the cursor keeps whatever is under the loupe fixed as it grows, so the glass reads as a real lens rather than a panning window, and the alignment holds at any size or zoom with nothing measured. A glass rim, a soft inner light, and a cast shadow sell the loupe, all pointer-events-none so it never intercepts a click, and the magnified layer is aria-hidden so the real children stay the single source of truth for assistive tech. It debuts as a Cards & surfaces experiment — a loupe over a fine-print plate that magnifies the site's own 'Made, not assembled.' claim set in tiny mono type, so bringing the glass close rewards you with detail that holds its edge. Under prefers-reduced-motion the loupe still works — it is direct manipulation, not autonomous motion — but the fade-and-scale entrance and the springy rim drop for an instant appearance." },
      { kind: 'component', text: 'GlitchText — a word that tears into digital signal corruption, the chromatic-glitch effect the type family was missing. Two colour-split ghost copies of the text — a cyan and a lime — sit exactly over a clean base and, during a burst, snap to sliced horizontal offsets under a screen blend, so the word shreds into shifted coloured channels and then re-forms. It sits deliberately apart from its three nearest neighbours, which all say "glitch" a different way: FuzzyText shears clean grayscale scanlines sideways like a detuned screen, LetterGlitch flickers a whole canvas grid of glyphs like a decode, and DecryptedText scrambles glyphs out of noise into place — this one keeps the word crisp and rips coloured channels off it in slices. Honest about the machine and to assistive tech: the base layer is untouched real text, always legible, selectable, and spoken once via aria-label, while the two ghosts are aria-hidden decoration that only appear while glitching; the slice motion is pure CSS keyframes snapped with steps(1) so the offsets cut rather than slide (a smooth tween would read as a wobble, not corruption), and React only toggles the burst class so nothing renders per frame. Three triggers — auto (bursts on an interval, each instance phase-offset so several on a page never strobe in lockstep), hover (glitches while hovered or keyboard-focused), and always. It debuts as a "Chromatic glitch" Type & text experiment beside the signal-fuzz tear so the two read against each other. Under prefers-reduced-motion the ghosts are dropped entirely — just the clean word, no timers and no listeners.' },
      { kind: 'polish', text: 'The case study, deepened into a real spec sheet. A project page used to run blurb → poster → stack → highlights → narrative; now, after the poster, an editorial "At a glance" panel lays out the facts as a divided definition list — Role, Year, Platform, Status — and rows appear only when the project actually carries that fact, so nothing is padded with a placeholder. Below the highlights, a new "What I did" section makes the work person-first: the honest division of labour, area by area, each row a lime label (Product, Design, Frontend, Native iOS, Backend for Guided; Design, Motion, Engineering for this site) and one true line about what these hands made — no metrics, no invented percentages — arriving on a staggered scroll-reveal with a lime edge that grows in on hover. It foregrounds the craft inside each project rather than only describing the product, and it makes "This site" read as a substantial case study standing beside Guided rather than a footnote. The quick-look modal picked up the same platform and status line so the fast preview and the full write-up never disagree, and Guided’s old "What I build" narrative — now said better by the structured breakdown — was dropped so the facts live in one place.' },
      { kind: 'component', text: "DynamicIsland — a status pill that changes its own SHAPE to fit whatever it is carrying, in the spirit of Apple's Dynamic Island, and the first surface in the library where the size and radius change IS the animation rather than a decoration on top of one. The library could already morph tabs (GooeyTabs), stack cards into a deck (ScrollStack), and open a modal, but nothing re-shaped itself between whole states, so this fills a real gap. It springs between four activity geometries — a resting notch with a dim camera dot and a lit sensor; an expanded now-playing bar with an album blob, title and artist, and a live waveform whose bars breathe on staggered loops; a countdown timer draining a lime ring off real elapsed time with mm:ss digits; and an incoming call with a pulsing avatar and real decline and accept buttons that end the activity. One spring drives the frame, AnimatePresence crossfades the inner faces with a soft blur-and-rise so a face rises into the pill as the outgoing one sinks, and the looping flourishes are the only motion added on top. It is honest to the machine and to assistive tech: the timer ring is genuine progress off elapsed time, the call buttons are real buttons, and a role=status / aria-live label narrates the current activity so the visual morph is spoken. It debuts as a full-width Navigation & controls experiment — a segmented control flips between idle, music, timer, and call, and the demo makes the point that the shape change is the motion. Under prefers-reduced-motion the shape change stays (it carries the information) but every loop stills — the waveform holds a fixed silhouette, the timer steps whole seconds, the call pulse rests — and the spring shortens so nothing overshoots." },
      { kind: 'component', text: 'Switch — a tactile on/off toggle, the one binary control the controls family was missing. ElasticSlider gives a continuous rubber-band range, Knob a 270° rotary dial, and Stepper discrete steps, but nothing covered the plain settings-row switch, so the Playground had been leaning on a hand-rolled CSS toggle; this replaces it with a real, extracted component. It speaks the same give-it-physics language the sliders do: the thumb rides a spring across the track instead of snapping, and while it travels it squashes and stretches in the direction of motion — scaleX up, scaleY down off the thumb’s own velocity, area roughly kept — so a flick lands with weight rather than a flat CSS transition. The rail crossfades from a muted bar to the lime accent on the same spring, and a small mark inside the thumb morphs from a dash to a check as it crosses. It is honest to the keyboard and assistive tech: a real role=switch — a focusable button carrying aria-checked, toggled by click or Space/Enter, with a visible focus ring, controlled or uncontrolled, and a disabled state — and because the geometry is fixed by props there is no measuring, the travel is exact. It debuts as a Navigation & controls experiment (a Wi-Fi toggle, a reduce-motion toggle, and a disabled “Locked on”) and takes over the toggle in the Stateful micro-interactions card. Under prefers-reduced-motion the squash and the long spring come off — the thumb moves near-instantly and it stays a plain, fully usable toggle.' },
      { kind: 'component', text: 'BlobCursor — a gooey blob that chases the pointer across a surface, the soft, physical kind of cursor response the field family never had. A short chain of solid circles trails the cursor: the lead eases toward the pointer and each follower eases toward the one ahead, and the whole chain renders under an SVG gooey filter — a Gaussian blur crushed by an alpha-ramp colour matrix, the same weld GooeyTabs and GooeyText already use — so the circles fuse into one liquid mass with a stretching tail rather than reading as separate dots. It sits deliberately apart from its nearest neighbours: MetaBalls drift on their own timeline with no pointer chase, Ribbons streak thin canvas trails, PixelTrail lights a grid of cells under the cursor, and Crosshair is a precise measuring reticle — this one is grabbable-feeling, swelling when you press and rippling the release down the tail. In the spirit of React Bits’ Blob Cursor, hand-built here and honest about the machine: one RAF loop writes translate3d straight onto the blobs with no per-blob React state, the filter id comes off a module counter (no Math.random, which is unavailable here, and no useId churn), it fills a positioned parent and only lives while the pointer is over it, drifting back to rest at the centre otherwise. Debuts as a full-width pointer-fields experiment on a cursor-none panel. Under reduced motion the loop never starts — a single still blob sits at the centre so the soft liquid shape still reads while nothing chases anything.' },
      { kind: 'component', text: 'InfiniteScroll — a vertical column of cards that loops forever and never seams, the endless-list primitive the library was missing. It sits deliberately apart from its nearest neighbours: Marquee runs a single horizontal track on pure CSS with no way in, and CircularGallery is a coverflow that snaps to the nearest card — this is a plain vertical column that drifts on its own and, when you grab it, scrubs under your finger and carries a decaying momentum on the release before easing back into the drift. In the spirit of React Bits’ InfiniteScroll, hand-built here and honest about the machine underneath: one RAF loop drifts the track and wraps it by exactly one sequence height — measured off the real DOM, the offset between the first card of one copy and the first of the next, so it holds at any font size or gap — and writes the transform straight onto the track, no per-item React state. An optional 3D lean, hover-to-pause, and soft top and bottom mask fades so rows dissolve at the edges rather than clipping. It debuts as a full-width Navigation & controls experiment: two looping columns of the disciplines I work across — Frontend, Native iOS, Motion, Backend, Applied AI, Design — one drifting up and one leaning and drifting down, so the demo says what the range is rather than resting on any one project. Under reduced motion the loop is dropped entirely for a plain, natively-scrollable column, fully legible, and only the first copy ever reaches assistive tech.' },
      { kind: 'component', text: 'ShinyText — the quiet cousin of the gradient wash. GradientText throws a bold white-to-lime shine across a headline; this keeps the copy dim and legible and lets a single soft band of light drift through it now and then, the way a small label catches the light — built for eyebrows, status lines, and datelines rather than display type. One horizontal gradient, a long flat base with a tight bright notch, background-clipped to the glyphs and slid across by the same shine keyframe GradientText already uses, so there is one source of that motion. A mono and a lime tone, and an adjustable sweep speed. It now lights the "Berlin" dateline on the Now page (below). Honest under reduced motion: no clip trickery frozen mid-sweep — the text simply rests at its muted base colour, fully readable.' },
      { kind: 'polish', text: 'The Now page, brought back to life. A now page is honest precisely because it goes stale, so rather than hide the age the dateline now reads it at load and says it out loud — "today", "3 days ago", "2 months ago" — computed from the last-revised date at render, warming to a gentle amber tag once the snapshot passes six weeks. The Berlin dateline picks up a ShinyText shimmer, and the focus items were revised and re-dated: the motion-design line now owns the real number — past a hundred hand-built components — rather than an old rounder figure.' },
      { kind: 'component', text: 'Highlighter — a marker-pen highlight that sweeps in behind (or under) a run of words, the emphasis primitive the type family was missing. Its signature is the stroke: a translucent lime band with soft, slightly uneven ends grows left-to-right from nothing, transform-origin left, like a highlighter dragged once across the line — a different mechanic from its two nearest neighbours, DrawSVG (which traces a real vector path into an underline) and SpotlightReveal (which uncovers hidden text under a moving cursor mask). In the spirit of Aceternity / React Bits’ Highlighter, hand-built here, and honest about it: the words stay real, selectable, copy-pasteable text on top, and the band is aria-hidden decoration painted behind — nothing is hidden from assistive tech to make the effect. inView, mount, and hover triggers; a behind-text band or a thinner underline; a small tilt so the mark reads hand-drawn rather than printed. It now marks two beliefs in the About intro — "feel effortless" and "small moments" each take a marker sweep as the page settles, so the page’s own claim about the quiet details is emphasised the way a person would, in ink. Debuts as a replayable Type & text experiment. Reduced motion paints the band fully and still — the emphasis lands, the animation does not.' },
      { kind: 'component', text: 'Meteors — a calm, directional rain of light on one canvas, the gentle streak the field family never had. It sits deliberately apart from its neighbours in the light band: Starfield warps stars radially out of a vanishing point, Lightning wriggles jagged filaments between anchors, Beams throw hard volumetric shafts — this is the diagonal kind, thin streaks with a bright head and a tail that fades to nothing, staggered so the sky is never empty and never marching in step. The honest engineering is in the recycle and the envelope: each streak crosses the box along one fixed travel angle and, when it runs off the far edge, respawns on the entry line with a fresh length, speed, and cross-offset, its brightness faded in over the first stretch and out near the end so a reset is never a visible pop. A seeded PRNG (mulberry32) gives the scatter a natural spread that still renders the same every mount — no Math.random reaching for entropy per frame — and there is no per-streak React state, one RAF loop, DPR-aware and ResizeObserver-driven. Most streaks are cool white with a lime minority for accent, and any streak whose head passes near the pointer warms toward lime and lengthens, the same "fields warm toward the cursor" language the rest of the library speaks. It now backs a new reel scene, "On pace" — a meteor shower carrying the reel’s one line about small, steady work reading like a shower rather than a flash, so the site’s "grows most days" ethos gets a moving surface and the reel de-centres any single project by a beat more (the reel now runs ten scenes, NN / 10). Debuts as a full-width Meteor field in the Playground pointer-fields band. Under reduced motion there is no loop at all — a single still frame lays a few faint diagonal streaks so the shape reads while nothing moves.' },
      { kind: 'component', text: 'BlurText — text that resolves out of a soft focus, one word (or character) at a time. The blur is its signature and its whole point: each unit starts heavily out of focus and drifts a little as it sharpens to nothing, so the line reads like a lens pulling in — a different feel from everything near it (SplitText lifts clean letters into place, ScrollReveal fades words in on scroll, DecryptedText resolves glyphs out of noise). In the spirit of React Bits’ BlurText, hand-built here: a Framer container staggers each unit, whole words stay unbreakable so lines wrap only between words, and an optional shine paints the lime gradient per unit. It now drives the Home Range heading — "More than one project." now pulls into focus out of blur, so the site’s clearest de-centring line is made in its own motion rather than merely written. Debuts as a replayable Type & text experiment. Honest to assistive tech via a real label with every split span hidden; reduced motion paints the finished line still, no blur and no stagger.' },
      { kind: 'component', text: 'AnimatedBeam — a beam of light that travels a curved line drawn between two real DOM nodes, the connective kind the library was missing. It is the cousin of BorderBeam: that one laps a rounded border, this one wires two separate things you can point at — the Magic-UI / React-Bits signature, hand-built here. The geometry is honest — it measures the container and both node elements with getBoundingClientRect and re-measures on resize, so the path is always drawn between wherever the nodes actually are — and the travelling light is a single SVG gradient whose endpoints sweep across the box, so nothing re-renders per frame. It now wires the new About "How it fits together" node web, where each discipline — Frontend, Native iOS, Motion, Backend, Applied AI, with the real tool named under each — beams into a central AC hub, so the range reads as one craft converging rather than five separate jobs (a small way to de-centre any single project). Debuts as a full-width Cards & surfaces experiment: three sources feed the hub and it fans out to two more, the light flowing through the whole graph. pointer-events-none and aria-hidden, so it never intercepts a click or speaks to assistive tech; reduced motion holds every beam as a faint resting line.' },
      { kind: 'component', text: 'DrawSVG — line art that draws itself, one stroke at a time. A genuinely different kind of motion from anything in the library: not a spring, a canvas field, or a text effect, but an outline that traces itself into being by animating the real path length (Framer’s pathLength, 0 to 1). Timeline and ScrollScene only scale a bar to fake a draw; this animates the actual curve, so any hand-authored shape — a swash, an orbit mark, a check, a monogram — appears as if drawn by a pen, rounded caps and all, one stroke after the next. It fires once as it scrolls into view (or on mount, or on a loop) and re-traces on hover. It now signs the About page: an editorial swash draws itself under the title, so the page’s claim to craft is made in a hand-drawn stroke rather than merely stated. Debuts as a Scroll-driven experiment showing four marks tracing on a loop. Honest to assistive tech via role and label; reduced motion paints every stroke finished from the first frame, nothing gated behind the motion.' },
      { kind: 'component', text: 'Crosshair — a precision reticle, a kind of pointer response the library did not have. Not the site cursor (a global dot and ring that follows the actual pointer) and not a canvas field — this is a DOM overlay of two spring-eased hairlines that span a section, meet at a lit node, and carry live X/Y readouts along a calibrated tick scale down the edges, so a surface reads as aligned and measured, the language of someone who builds carefully. It listens on the window and positions from its own rect, so the lines glide toward the cursor as it crosses the whole surface and rest at centre when it leaves; the numeric readouts are written straight to the DOM off the springs, so a moving cursor never triggers a render, and the whole overlay is pointer-events-none, so it never intercepts a click. It backs the Colophon closing note — the engine-room page ends on the language of alignment, its hairlines finding the cursor as you read the invitation to the source — and debuts as a full-width pointer-fields experiment in the Playground. Reduced motion and coarse pointers get a single still, centred crosshair — no listeners.' },
      { kind: 'polish', text: 'The library, up close — every card on /library now opens a quick-look modal instead of some linking away and others sitting inert. A tap brings up the component on its own: its kind, its honest one-line note, its scan tags (now clickable, they set the page filter), and — where the piece is actually on show — a button straight through to that place. Prev and Next page through whatever is currently filtered in, arrow keys included, with a live position count, so you can walk the whole catalogue or just one kind without closing the sheet. It makes all of the components explorable in place, not only the ones that happened to have a home, and gives the catalogue the same in-page popover the rest of the site leans on.' },
      { kind: 'component', text: 'FlowField — the library had grids, needles, cells, threads, and blobs that bend toward the pointer, but nothing that read as moving current. This one advects a few hundred motes through a smooth vector field whose angle is a cheap wrap of sines of position and time — no noise library — and lets each mote leave a fading trail, so the canvas streams rather than scatters. Within reach of the cursor the field angle blends toward the tangent around the pointer, so the flow stops running straight through and curls into a lime eddy that the cursor drags along, brightening as it goes. Honest to the machine: one canvas, one RAF loop, motes recycled when they age out or drift off-frame, no per-mote React state. It debuts as a full-width pointer-fields experiment in the Playground. Reduced motion never starts the loop — it traces each streamline forward through the frozen field once, so the current’s topology is legible while nothing moves.' },
      { kind: 'component', text: 'Toast — the site’s missing notification primitive. A provider at the top of the Layout shell owns a shallow queue and hands any page, dialog, or command a toast() call; the messages render through a portal into an aria-live region pinned to the bottom edge, so a screen reader hears each one and nothing is clipped by an overflow-hidden parent. The mechanism is deliberately honest: when motion is allowed, a single CSS animation both depletes the lime meter and, on its onAnimationEnd, dismisses the toast — the bar and the clock are literally the same thing, so they can never drift, and hovering pauses both at once; the stack caps at three, so the oldest steps off the top rather than piling up. Each toast springs up from the edge, can be flicked aside, and carries a confirmation tick or a gentle error mark. It now backs the real "email address copied" confirmation from both the contact channels and the command palette — so the confirmation still lands even after the surface it came from closes — and debuts as a Navigation & controls trigger board in the Playground. Reduced motion drops the drift and the travelling meter for a clean fade on a plain timer (and never mounts the CSS meter, so the global reduced-motion collapse of animation-duration can’t fire it early).' },
      { kind: 'page', text: 'The reel (/reel) — an art-directed showreel, and the counterpoint to the Playground. Where the Playground dissects motion into a labelled workbench of controls, the reel runs nine full-screen scenes as a designed sequence: each pairs one signature hand-built backdrop (Aurora, MeshGradient, Beams, Threads, Lightning, Iridescence, Particles, Orb, Squares) with a single line of how I think about making things — motion, light, line, energy, colour, systems, form, structure. It is a small manifesto more than a pitch: it centres the point of view about craft, not any one project, so it de-centres the client work by design, and closes by handing off to /work and the Playground. Snap-scrolled (proximity, so the footer is never trapped), navigable by arrow keys, j/k, Home/End, and a dot rail, with a live NN / 09 counter. Each heavy backdrop mounts only while its scene is on or beside the screen — an IntersectionObserver tracks the scene crossing the viewport midline — so at most a couple of canvases ever run at once and the route chunk stays under 4kB gzipped. Reduced motion drops the scroll-snap and the scene reveals; every backdrop already stills itself.' },
      { kind: 'component', text: 'Aurora gains a scoped mode — an optional prop flips the ambient field from the fixed site-wide layer to an absolute fill of a positioned parent, so the real component can back a single full-bleed reel scene instead of the viewport. The site-wide use in Layout is untouched.' },
      { kind: 'component', text: 'AnimatedList — a navigation list with life. Rows stagger in one after the next as the list scrolls into view, and a single lime highlight springs between rows (a shared layoutId, so it glides its own box rather than every row fading a background) as you hover or move focus through them. Deliberately not an ARIA role game: the rows stay ordinary, individually-focusable links or buttons — so the list reads and tabs exactly as a list should — with arrow keys, Home, and End added purely as an enhancement on top. It debuts driving the Home page’s new "In the open" section (below), and as a Navigation & controls experiment of real routes. Reduced motion drops the stagger and the glide — the highlight just appears on the active row.' },
      { kind: 'page', text: 'Home "In the open" — a new closing section, between Explore and the contact band, that surfaces the most recent build-log headlines as a compact AnimatedList and links straight through to /changelog. It turns the "open source, grows most days" promise into something you can see the moment you reach the bottom of the page, and de-centres any one project by making the site’s own steady making the last note. The headlines live in a standalone data/latestLog.ts on purpose: importing them from the (large) changelog module would have dragged the whole log text into the eager Home chunk and undone the code split — this way the initial bundle stays flat.' },
      { kind: 'component', text: 'Tooltip — a small accessible hint that rises on hover and on keyboard focus and closes on leave, blur, or Escape. Unlike a naive wrapper-relative hint, the bubble is rendered through a portal as a fixed-position element and placed from the trigger’s measured rect, so it is never clipped by an overflow-hidden ancestor or trapped under a lower stacking context — and it flips to the opposite side and clamps inside the viewport when the preferred placement would run off the edge, with the arrow tracking the trigger’s centre, so an edge-hugging nav button still gets a fully visible hint. The trigger carries aria-describedby, so assistive tech announces the hint with the control. It now backs the nav’s terse buttons across the whole site — the search/command button, the get-in-touch button, and the wordmark — and debuts as a Navigation & controls experiment showing its four placements and the off-screen flip. Reduced motion just fades it, no travel' },
      { kind: 'component', text: 'TextType — a typewriter, the classic terminal cadence the library was missing. A phrase types out one keystroke at a time, holds, erases itself, and the next takes its place, a blinking block caret riding the end throughout. It is a different kind of text motion from everything around it: DecryptedText resolves glyphs out of noise, GooeyText melts one word into the next, SplitFlap hinges a whole glyph, SplitText lifts letters into place — this one is keys appearing and erasing. Honest to the machine underneath: no per-frame React state, just a single self-rescheduling timeout that advances one character per tick and clears on unmount, and the caret blink is a CSS keyframe so the reduced-motion guard stills it for free. It types a live prompt line on the /terminal page — arseniy@portfolio ~ % cycling through the real commands it accepts (help, whoami, ls, open work, stack, contact) — and debuts as a Type & text experiment. Honest to assistive tech via a full aria-label; reduced motion renders the first phrase in full with a steady caret' },
      { kind: 'component', text: 'Masonry — a real masonry wall, not a fixed grid. A single ResizeObserver measures the container and every tile, and each one is absolutely positioned into the currently-shortest column from its own measured height, so content of any height packs tight with no gaps and nothing is forced onto a shared row. Change the width or shuffle the tiles and the whole wall reflows on a spring instead of snapping; each tile lifts in the first time it lands. No per-frame React state — the layout is a memo over measured heights, recomputed only when a height or the width actually changes. It debuts as a full-width Cards & surfaces experiment holding an editorial wall of the site’s own making-by-hand ethos — 97 components, spring over tween, reduced motion always, no template — so the demo says what it is. Reduced motion drops the springs and the reveal and places the same packed layout instantly' },
      { kind: 'component', text: 'MeshGradient — a living wash of colour you steer. Five large, heavily-blurred radial blobs — lime, emerald, sky, and violet — sit at fixed anchors and each wanders on its own slow idle loop, so the field is never still; on top of that the whole mesh leans toward the pointer, every blob parallaxing by its own depth so the colour seems to gather under the cursor and warm as it nears. Distinct from its neighbours in the pointer-fields band: Aurora is a fixed ambient drift with no pointer, Iridescence is an oil-slick sheen, Beams are hard light shafts — this is a warm, liquid backdrop you can sit a headline straight on top of. Pure layered gradients moved by transforms, no canvas and no per-frame React state, so it stays cheap. It now backs the About "What I am about" manifesto — the site’s clearest statement of craft-over-any-one-project, given a living surface — and debuts as a Colour field in the Playground pointer-fields band. Reduced motion holds a balanced, still mesh with the lean off' },
      { kind: 'polish', text: 'Grain — a film-grain texture now lies over the whole site. A small tile of random grayscale bytes is reshuffled a few times a second and repeated across the surface as a canvas pattern, so a full-viewport grain costs one tiny noise buffer per refresh rather than a per-pixel repaint. It composites under content through soft-light at a whisper of opacity, so mid-gray reads as neutral while brighter specks lift and darker ones sink — the dark theme picks up a fine, printed, editorial feel instead of flat black. pointer-events: none and above content but below the nav and cursor, so it never interferes; a Playground specimen shows it turned up. Reduced motion holds a single still frame' },
      { kind: 'component', text: 'GridMotion — a living wall of tiles. Five rows drift sideways at their own pace and in alternating directions (the same seamless duplicate-and-slide loop Marquee runs, but stacked and staggered into a field), while the whole slab tilts in 3D toward the cursor like a panel you are leaning over — sprung, so it eases in and settles back rather than snapping. Distinct from the 1D text bands (Marquee, ScrollVelocity, CurvedLoop): those are single tracks, this is a two-axis surface you steer. Fed a vocabulary rather than one message — the disciplines and tools I work across, Frontend / Native iOS / Backend / Applied AI / Motion accented among React, SwiftUI, Supabase, and the rest — so it reads as range, no single tile carrying the story. It debuts as a Range field in the Playground pointer-fields band. Reduced motion holds the wall still and square, every tile there and legible' },
      { kind: 'component', text: 'GooeyText — one word melts into the next: two text layers sit under a gooey SVG filter (a Gaussian blur, then an alpha-crushing matrix — the same metaball trick GooeyTabs plays on its blobs), so as one word blurs out and the next sharpens in, their glyph-halos fuse into a single liquid mass at the midpoint before resolving into clean type. The alpha-crush eats thin strokes, so it engages only inside a morph — at rest the word renders with no filter and stays perfectly crisp. Not a crossfade but a physical morph, and distinct from GooeyTabs (a moving pill) and MetaBalls (drifting blobs) — here the fusing shapes are letters. It drives a new display-size "I work across Frontend / Native iOS / Backend / Applied AI / Motion" band on the About page — foregrounding the range rather than any one project — and is a Type & text experiment. Honest to assistive tech via an aria-live region; reduced motion drops the filter for an instant, legible swap' },
      { kind: 'component', text: 'HoverIndex — an editorial index where hovering a row floats a generative preview panel that trails the cursor with a little spring lag, the awwwards-style hover-reveal list. A different kind of thing from the rest of the library: not a field, a card, or a text effect, but a list whose entries summon a floating thumbnail (distinct from FlowingMenu, whose lime panel slides in and stays inside the row). The previews are honest brand art generated per label, not screenshots, and every row is a real link, so it doubles as navigation. It closes the Work ledger with an outward index into the rest of the site — About, Playground, Toolkit, On motion, Writing, Now — framing the projects as one facet rather than the whole story, and debuts as a Playground controls experiment fed the real site links. Touch and reduced motion drop the floating preview for a clean, fully legible list' },
      { kind: 'component', text: 'Odometer — a mechanical trip-meter count: each place is a vertical reel of 0-9 clipped to a single-digit slot, and on reveal every reel slides up from zero into place, passing through the numbers in between the way a car odometer does, with higher places landing a beat after the ones to their right. A physical slide, not a text-swap — a different kind of count from the AnimatedCounter that tweens a number or the SplitFlap that hinges a glyph. It now rolls the by-the-numbers figures on the homepage and is a Playground experiment; reduced motion parks every reel on its final digit instantly' },
      { kind: 'component', text: 'Knob — a tactile rotary control you turn like hardware: drag up or down to sweep the pointer around a 270° arc, spin the wheel for fine steps, or focus it and use the arrow, Page, and Home/End keys. The pointer rides a spring so it settles with a little physical give, and it is a real ARIA slider throughout. It gave the Design page’s Motion section its missing verb — a two-knob spring tuner where you turn stiffness and damping and watch a puck spring across on your own curve, the "held back for the physical bits" note finally made playable — and drives a live meter in the Playground controls band. Reduced motion snaps the pointer exactly and calms the puck' },
      { kind: 'page', text: 'The library (/library) — the site claims to be made, not assembled, so here is the proof laid out flat: every hand-built component in the repository, catalogued one by one into kinds (type, surfaces, pointer fields, scroll, controls, overlays, objects, foundations), each with an honest one-line note and its scan tags. A live search and kind chips filter it, tags are clickable, and where a component is actually on show a card links straight there. Backed by a glyph field; reduced-motion aware. Sourced from data/library.ts, which is kept in step with the component count' },
      { kind: 'infra', text: 'data/stats — reconciled the honest component count to the literal number of files in src/components (the two had drifted), so the Home numbers band, Colophon, Contents, Changelog, and the new library all read one true figure' },
      { kind: 'component', text: 'Globe — a turning globe on a plain 2D canvas, no WebGL and no map texture: a Fibonacci lattice of dots rotated in 3D each frame, near-face bright and the far face a faint glass haze, so the ball reads as solid. Real cities sit at their true latitude and longitude as lime pins that pulse, label themselves, and fade as they turn to the back; it drifts on its own and a flick carries an inertial spin. Backs the About At-a-glance snapshot — the turning globe pins Berlin right beside the live local clock — and is a Playground field with Berlin, New York, and Tokyo. Reduced motion holds a still frame, Berlin facing front' },
      { kind: 'component', text: 'CompareSlider — a before/after seam you drag to wipe between two states: the top layer is clipped to a vertical divider while the finished layer shows through to its right, and a spring eases the seam toward your pointer rather than snapping. Click anywhere on the frame or focus the handle and use the arrow keys; both children stay in the DOM, so it is legible and a real ARIA slider. Anchors a fifth Craft principle — Structure, then surface, the same card as bare frame and finished self — and is a Cards & surfaces experiment; reduced motion drops the spring and the entrance sweep but keeps the drag' },
      { kind: 'component', text: 'Clock — a hand-drawn SVG analog dial that reads real Berlin time, DST-safe via Intl. One RAF loop sweeps the second hand continuously and writes transforms straight to the hand nodes, so there is no per-frame React render; the twelve-marker and second hand warm to lime when the local hours are awake, and reduced motion drops the sweep to an honest once-a-second tick. The site showed Berlin as digits in a dozen places — this gives it a face, on the Contact card and in the Playground' },
      { kind: 'component', text: 'Starfield — a warp field of points streaming out of a vanishing point and toward you, each a short streak so it reads as travel through space, not a twinkle; the vanishing point eases after the cursor so the whole field banks the way you point, and the nearest streaks warm to lime. Backs the Résumé header — the long view behind a one-page summary — and is a Playground field' },
      { kind: 'component', text: 'ProjectQuickLook — a shared project preview modal: the poster, role, blurb, stack, and highlights in a fast pop-over that always offers a way through to the full case study. Opened from the work ledger and the home cards, it replaced the one-off modal that used to live inside the home page, so both surfaces now share one honest component' },
      { kind: 'polish', text: 'Footer — the one un-crafted surface, a run-on paragraph of links, rebuilt as a proper closing statement: a get-in-touch CTA, a grouped map of every route, a live Berlin clock with an awake/asleep sense, and the keyboard hint. Present on every page, so the site now ends the way it begins' },
      { kind: 'component', text: 'Lanyard — a name badge on a cord you grab, throw, and watch swing to rest: a single constrained mass (an angle and an angular velocity) driven as a damped pendulum, with the badge as real crisp HTML that tilts with the cord. A different kind of motion from the fields and cards — one weight on a string' },
      { kind: 'component', text: 'Squares — a structural lattice field on one canvas: a soft band of light drifts along the diagonal forever while the cells under the cursor warm lime, lift their stroke, and bloom. Backs the Index header' },
      { kind: 'component', text: 'SphereMenu — a draggable 3D sphere of links you grab and spin: points on a real Fibonacci sphere, projected to 2D each frame so the labels always face you while the ones turned away shrink and dim. Every label routes into the site, so no one project sits at the centre' },
      { kind: 'motion', text: 'Home — a By-the-numbers band that turns the by-hand ethos into concrete, animated counts (components, pages, zero UI kits, one pair of hands), each figure literally true of the repo' },
      { kind: 'infra', text: 'data/stats — the site’s own counts moved to a single source of truth, so the home band, Colophon, Contents, and Changelog all read one number' },
      { kind: 'component', text: 'FlipCard — a card that physically turns over between two faces, a real half-rotation with the far face hidden; distinct from a dissolve or a cross-fade' },
      { kind: 'component', text: 'FuzzyText — clean type torn into analog signal-fuzz scanline by scanline, harder under the pointer; it turns the 404 number into a lost signal' },
      { kind: 'component', text: 'ScrollScene — a sticky scrollytelling stage that transforms as steps scroll past; on the Colophon it assembles a screen’s real layers one at a time' },
      { kind: 'component', text: 'Iridescence — a breathing, palette-tinted sheen computed on a tiny buffer and blown up smooth; the cursor spreads a ripple through it. Backs the On-motion title' },
      { kind: 'page', text: 'Design language — a living style guide of the site’s own tokens: three colours, two faces, one curve, each one copyable' },
      { kind: 'page', text: 'Index — an editorial, searchable table of contents for the whole site, grouped into sections with live counts' },
      { kind: 'component', text: 'CurvedLoop — a line of type that rides a curved path, looping forever; grab it to scrub and flick it to fling' },
      { kind: 'component', text: 'ScratchReveal — a canvas scratch-off foil you drag away to uncover the card beneath' },
      { kind: 'component', text: 'Particles — a drifting constellation field that webs and warms toward the cursor' },
      { kind: 'page', text: 'Terminal — an interactive shell that drives the whole site by typing' },
      { kind: 'page', text: 'On motion — a page where four beliefs about craft are playable, not stated' },
      { kind: 'component', text: 'ASCIIText — a word rendered as a live, cursor-lit ASCII field' },
      { kind: 'component', text: 'ChromaGrid — a torch-in-the-dark card grid that lights under the pointer' },
      { kind: 'component', text: 'Folder — a tactile folder that fans its papers out as links' },
      { kind: 'component', text: 'Stepper — an accessible step-through with a sweeping lime rail' },
    ],
  },
  {
    marker: '04',
    title: 'The component library',
    summary:
      'A deep bench of motion primitives, each built from scratch rather than pulled off a shelf — the raw material the pages are assembled from. This is where most of the craft lives.',
    items: [
      { kind: 'component', text: 'Backgrounds: Aurora, Beams, Threads, Lightning, MetaBalls, Ribbons, DotGrid, MagnetLines' },
      { kind: 'component', text: 'Text in motion: GradientText, RotatingWord, SplitText, DecryptedText, ScrollVelocity, TrueFocus, VariableProximity' },
      { kind: 'component', text: 'Surfaces: SpotlightCard, TiltCard, GlareHover, GlassSurface, BorderBeam, BentoGrid, CardStack, ScrollStack' },
      { kind: 'component', text: 'Pointer play: Cursor, ClickSpark, PixelTrail, Gravity, Lightbox, Dock, MagneticButton' },
      { kind: 'component', text: 'Structure: Marquee, HorizontalScroll, CircularGallery, FlowingMenu, Timeline, Accordion, GooeyTabs' },
    ],
  },
  {
    marker: '03',
    title: 'From one page to a whole site',
    summary:
      'A single scrolling page became a real multi-page site: client-side routing, a shared animated shell, and a set of ways to move through it that reward keyboard and mouse alike.',
    items: [
      { kind: 'page', text: 'About, Work index and per-project case studies, Toolkit, Now, Writing, Contact, Answers, Colophon, Résumé' },
      { kind: 'infra', text: 'react-router with a shared Layout, deep-linkable routes, and code-split page chunks' },
      { kind: 'motion', text: 'Page transitions on every navigation via AnimatePresence' },
      { kind: 'component', text: 'CommandPalette — a fuzzy launcher for every page, project, and action' },
      { kind: 'component', text: 'Keyboard chords — tap g then a key to jump anywhere; press ? for the map' },
      { kind: 'polish', text: 'Nav with active states, a mobile menu, a footer, back-to-top, and scroll progress' },
    ],
  },
  {
    marker: '02',
    title: 'An editorial voice',
    summary:
      'The look moved past a default all-sans template. A self-hosted variable serif gives headlines a considered, editorial tone, and the dark theme with its single lime accent settled in.',
    items: [
      { kind: 'polish', text: 'Fraunces, a self-hosted variable serif, on display headlines with live optical sizing' },
      { kind: 'infra', text: 'Preloaded latin subset so headlines paint without a flash of fallback' },
      { kind: 'polish', text: 'Dark theme and the lime accent as a consistent, restrained system' },
      { kind: 'component', text: 'Preloader, Eyebrow, Seo, and the shared Reveal used across every page' },
    ],
  },
  {
    marker: '01',
    title: 'The foundation',
    summary:
      'The first commits: a strict, fast toolchain and the first hand-built motion, chosen so the site could itself be a sample of the work rather than a description of it.',
    items: [
      { kind: 'infra', text: 'Vite, React 18, and TypeScript in strict mode' },
      { kind: 'infra', text: 'Tailwind v4 via the official Vite plugin, no config-file content globbing' },
      { kind: 'motion', text: 'Framer Motion as the single animation layer, with prefers-reduced-motion honoured throughout' },
      { kind: 'component', text: 'The first primitives: Aurora, HeroOrbit, SpotlightCard, MagneticButton, Marquee, ScrollCue' },
    ],
  },
]

/** Total concrete items across all chapters — a rough measure of ground covered. */
export const SHIPPED_COUNT = CHAPTERS.reduce((n, c) => n + c.items.length, 0)
