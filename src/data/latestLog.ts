// A compact, standalone list of the most recent build-log headlines, for the
// Home page's "In the open" section.
//
// Why its own tiny module and not derived from `changelog.ts`: the Home chunk
// ships eagerly, while the full Changelog page is lazy-loaded. If Home imported
// anything from `changelog.ts`, that (large) module would be shared with the
// eager entry and Rollup would hoist the whole log — every long entry string —
// into the initial bundle, undoing the code split. This file imports nothing
// heavy, so the headlines cost almost nothing up front.
//
// It mirrors the top of `CHAPTERS[0]` in `changelog.ts` — keep the two in step
// (the daily routine edits the log each run anyway). The full sentences, the
// kinds, and the filter all still live on `/changelog`.
//
// Kept to the ~10 most recent, as short one-line headlines: the Home section is
// a teaser ("the most recent of them") with a "Read the full build log" link,
// and every title here ships in the eager bundle — so this stays a tight,
// glanceable list, not the whole archive re-stated in full paragraphs. When you
// prepend a new entry each run, drop the oldest so the list does not grow
// unbounded and the landing page stays premium.

export interface LogHeadline {
  /** The short name of the thing that shipped. */
  title: string
  /** The kind label shown as a chip, matching KIND_META in changelog.ts. */
  tag: string
}

export const LATEST_LOG: LogHeadline[] = [
  { title: "A Zoetrope — the Objects & toys family gains the oldest moving picture there is, rebuilt so the trick itself is on screen. A disc carries a ring of twelve drawn frames — the twelve poses of one bounce of a ball — with a radial slit between each pair, and glancing through the slit at the top window the eye stitches the passing frames into a single animation: the ball comes to life and bounces in place. The honest, interesting part is that a screen's motion is really a sequence of stills shown fast, which is exactly what this device makes visible, and the state proves it: the disc's rotation is the only thing stored, and the frame at the window — and so the pose the big viewer reconstructs — is derived from that rotation and nothing else, so the picture can never drift from where the disc actually is. The viewer and the twelve cells on the disc are all drawn by one ballPose function, so what the slit reveals is provably the same drawing that rides the disc. Drag the disc to spin it and it coasts on real momentum, the velocity bled off exponentially the way a spun wheel loses speed to friction; press Spin for a shove, or toggle 'Look through the slit' to darken all but the top wedge and watch the strobe do the work. A real role=slider over the twelve frames: the arrow keys tick one frame at a time, Home returns to the first, and a live region reads the current frame. Debuts full-width in the Playground's Objects & toys family after the Sliding puzzle. Under prefers-reduced-motion there is no free coast — a drag tracks the finger and stops on release, and Spin and the arrows step frame by frame. The component count moves to 252.", tag: 'Component' },
  { title: "A Micrometer — the controls family keeps rebuilding physical instruments as things you use rather than pictures of them, and this one is the finest measurer of the set: where the Vernier caliper reads a length to a tenth of a millimetre by an aligned division, a micrometer reads to a hundredth by a precision screw. Drag the thimble down the sleeve, or focus it and tick with the arrow keys, and the spindle closes onto a workpiece whose width is the reading, taken the way the real tool is with both halves on screen at once — the whole-and-half millimetres from the last sleeve graduation the thimble's edge has uncovered, and the hundredths from the one division on the fifty-part rim sitting on the sleeve's datum line, that division lit lime and the sleeve graduation it belongs to lit to match. The mechanism is the screw: the thimble turns on a half-millimetre pitch, so one full turn advances the spindle by exactly half a millimetre and the rim resolves that half to a hundredth, which is why the two scales always sum to the reading. The whole state is one integer count of hundredths, so sleeve plus thimble is exactly the reading. A real role=slider: arrows nudge a hundredth, Shift a tenth, PageUp and PageDown a full turn, Home closes the spindle and End opens it, and a live region reads the measurement. The thimble tracks the pointer one to one and eases only on a keyboard nudge; under prefers-reduced-motion even that cuts straight to the value. Debuts full-width in the Playground's controls family after the Protractor. The component count moves to 251.", tag: 'Component' },
  { title: "A Protractor — the controls family has been rebuilding physical instruments as things you use rather than pictures of them, and this one completes a small set: after the caliper that reads a length and the level that reads level comes the tool that reads an angle. A half-circle protractor: swing the arm around the dial, or focus it and tick with the arrow keys, and it reads off the double scale printed on it — an outer run 0 to 180 from the right base and an inner run the other way, so a line reads from either edge and the two readings always sum to 180, both on screen with the one major division the arm sits nearest lit lime on both scales at once. The angle is the single piece of state and the arm, the lit division, and both readouts all derive from it, so the number can never disagree with where the arm points. A real role=slider: arrows nudge a degree, Shift ten, Home and End take the ends, Enter squares it to ninety, and a live region reads both scales. The arm tracks the pointer one to one and eases only on a keyboard nudge or the Square-it reset; under prefers-reduced-motion even that cuts straight to the value. Debuts full-width in the Playground's controls family after the Slide rule. The component count moves to 250.", tag: 'Component' },
  { title: "A Slide rule — the controls family keeps rebuilding physical instruments as things you use rather than pictures of them, and after the caliper that reads a length comes the tool that does arithmetic. A slide rule reduced to the one thing it does best: multiplication on two logarithmic scales. The fixed D scale lives on the body, the sliding strip carries the C scale; slide the C index over one number on D, drag the cursor hairline to a second number on C, and the hairline crosses their product on D. Why it works is on screen rather than asserted — a value sits at a distance proportional to its logarithm, so sliding one scale along the other adds those distances, and adding logarithms multiplies the numbers — with the multiplicand under the C index and the product under the cursor both lit lime on D at once. Both moving parts are real role=sliders: the slide sets the multiplicand, the cursor picks the multiplier and reads the product, each tracking the pointer one to one and easing only on a keyboard nudge, arrows a tenth, Shift a whole unit, Home and End to the ends, a live region narrating the product. The number is read off the geometry rather than stored, so it can never disagree with where the lines fall, and it is kept to one decade so a product that would run off simply stops at the far index. Debuts full-width in the Playground's controls family after the Vernier caliper. Under prefers-reduced-motion even the keyboard ease cuts straight to the value. The component count moves to 249.", tag: 'Component' },
  { title: "A Vernier caliper — the controls family keeps rebuilding physical instruments as things you hold rather than pictures of them, and after the spirit level's bubble comes the tool that reads a length. A metric vernier caliper, in real millimetres: drag the sliding jaw along the beam, or focus it and tick with the arrow keys, and the jaws open around a workpiece whose width is the reading. The honest, interesting part is that it is read the way the real tool is — the whole millimetres come from where the vernier's zero sits on the fixed scale, and the tenths from which of the ten vernier divisions lines up with a main-scale tick, that one coinciding division lit lime on both scales at once, so the trick that lets a coarse scale read a fine one is on screen and not merely asserted. The vernier is ten divisions over nine millimetres, so each is 0.9 mm and steps a tenth of a millimetre out of phase per tick; that tenth is the resolution, and because the whole state is one integer count of tenths, exactly one division can ever coincide. A real role=slider — arrow keys nudge a tenth, Shift a whole millimetre, Home closes the jaws and End opens them — with a live region reading the measurement. Debuts full-width in the Playground's controls family after the Rotary dial. The jaw tracks the pointer one to one; only a keyboard nudge eases, and under prefers-reduced-motion even that cuts straight to the value. The component count moves to 248.", tag: 'Component' },
  { title: "A Sliding puzzle — the Objects & toys family gains the classic fifteen-puzzle, rebuilt as a real object rather than a graphic. Fifteen numbered tiles and one gap sit in a square frame, and the only legal move is to slide a tile that shares an edge with the gap into it. Click such a tile, or drive the whole board from the arrow keys — each arrow pulls the tile on that side of the gap in, the way a thumb nudges the board rather than moving the hole. The honest, interesting part is the shuffle: every scramble is a long random walk of legal slides out from the solved board, each step never immediately undoing the last, so the board it hands you is always solvable and never a trivial one- or two-move mess — no parity trick, just the same physics that solves it running backwards. The tiles are the whole state, one array of sixteen values where zero is the gap, and each tile springs from its old cell to its new one, so a move reads as the tile travelling rather than blinking across; a counter tallies your slides and the whole board locks to lime the instant one to fifteen fall back into reading order. Debuts full-width in the Playground's Objects & toys family after the Abacus. Honest to a screen reader as a labelled grid whose gap is a live status line, and under prefers-reduced-motion the tiles cut straight to their cells with no glide. The component count moves to 247.", tag: 'Component' },
  { title: "A Rotary dial — the controls family keeps rebuilding physical instruments as real controls rather than pictures of them, and this one is a working rotary telephone dial. Put the pointer in a numbered hole and drag it clockwise to the finger stop, then release: the wheel unwinds under a governed spring at a constant tick rate and the digit lands in the readout as the hole passes home. The honest, interesting part is the encoding — the pull is metered in equal steps, so the digit you dial equals the number of steps travelled, which means dialling 1 is a flick and dialling 0 is very nearly a full turn, exactly as the old mechanism worked. The drag tracks the pointer's angle one-to-one and cannot be pulled past the stop, the way a finger meeting the metal tab cannot, and a pull that never reaches the stop winds back with nothing dialled. It is a real control beyond the drag too: click a hole or type 0–9 to dial it, Backspace rubs out the last digit, Escape clears the line, and each dialled digit is announced to assistive tech. Debuts full-width in the Playground's controls family after the Bubble level. Under prefers-reduced-motion the wheel never spins — a dialled digit simply appears — so it stays fully usable with no rotation at all. The component count moves to 246.", tag: 'Component' },
  { title: "A Bubble level — not a control the platform ships badly but a physical instrument, a carpenter's spirit level rebuilt as something you hold rather than a picture of one. Drag the milled body and it rotates rigidly under your hand — a tool you are holding should not feel elastic, so the frame tracks the pointer one-to-one — while the bubble trapped in the glass vial lags behind on its own spring, sliding toward the high end exactly as air would. The lag is the whole illusion: the rigid body carries the tilt instantly and the liquid catches up a beat later, its travel scaled by the sine of the pitch so it eases toward the ends the way a real curved vial reads. Bring it within a third of a degree of flat and the vial locks to lime, the bubble settling dead-centre between the two gauge lines, and a live region announces Level; a monospace readout shows the pitch to a tenth of a degree and names the low side. It is a real role=slider: focus the body and the arrow keys nudge the tilt, Shift takes a whole degree, Home or Enter levels it with a short spring settle, End pins it to the limit. Debuts full-width in the Playground's controls family after the Split pane. Under prefers-reduced-motion the bubble and the reset cut straight to their exact positions, the reading still perfectly true. The component count moves to 245.", tag: 'Component' },
  { title: "A Split pane — the resizable split view from every code editor, mail client, and file manager, and one more of the hard native things the controls family keeps rebuilding honestly because HTML never shipped it. Two panes share a container and a draggable divider trades space between them, one-to-one with the finger. The whole split is one number — the first pane's flex-basis as a percentage of the container — so nothing is measured per frame and the panes can never disagree. The part most versions skip is that it is a real control for the keyboard and screen readers, not a mouse-only trick: the divider is a genuine WAI-ARIA separator carrying its orientation and a live aria-valuenow of the first pane's percentage, so a reader hears 'separator, 52 percent' and it is fully drivable without a pointer — the arrow keys nudge it, Shift and the Page keys jump in tens, Home and End slam it to the limits, and Enter or a double-click snaps it back to the middle. A drag stays strictly one-to-one; only a keyboard nudge or a reset eases the basis, and it works both side by side and stacked. Debuts full-width in the Playground's controls family after the Combination lock, with a live percentage on the first pane and a toggle between side-by-side and stacked. Under prefers-reduced-motion the layout still resizes, it just cuts rather than glides. The component count moves to 244.", tag: 'Component' },
  { title: "A Combination lock — the controls family keeps rebuilding the inputs the platform ships badly, but this one is not a form field at all: a briefcase combination padlock, built as a tactile little machine. Three number reels sit in a milled lock body, and each is a real spinbutton — drag it vertically, roll the wheel over it, or focus it and tick with the arrow keys, Home and End. The digit you turn to slides in from the direction the wheel moved, up from below when you count up, over two faint neighbours behind an edge fade, so the slot reads like a physical reel rather than a number swapping. Land all three on the combination and the shackle lifts and swings clear on its hinge, a spring with real overshoot; spin any reel away and it re-seats at once. The honest part is that the code is shown as a hint, so the lock is actually openable rather than a locked box with no key, and a Scramble spins the reels to a fresh set guaranteed not to be the answer. Debuts full-width in the Playground's controls family after the Mood slider. Under prefers-reduced-motion the shackle and the reels snap to their exact states with no travel, it stays fully keyboard-operable, and a live region announces locked and open — nothing here secures anything, it is a toy about the feel of a lock. The component count moves to 243.", tag: 'Component' },
]
