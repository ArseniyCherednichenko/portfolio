// A compact, standalone list of the most recent build-log headlines, for the
// Home page's "In the open" section.
//
// Why its own tiny module and not derived from `changelog.ts`: the Home chunk
// ships eagerly, while the full Changelog page is lazy-loaded. If Home imported
// anything from `changelog.ts`, that (large) module would be shared with the
// eager entry and Rollup would hoist the whole log — every long entry string —
// into the initial bundle, undoing the code split. This file imports nothing
// heavy, so the headlines cost almost nothing up front.
//
// It mirrors the top of `CHAPTERS[0]` in `changelog.ts` — keep the two in step
// (the daily routine edits the log each run anyway). The full sentences, the
// kinds, and the filter all still live on `/changelog`.
//
// Kept to the ~10 most recent, as short one-line headlines: the Home section is
// a teaser ("the most recent of them") with a "Read the full build log" link,
// and every title here ships in the eager bundle — so this stays a tight,
// glanceable list, not the whole archive re-stated in full paragraphs. When you
// prepend a new entry each run, drop the oldest so the list does not grow
// unbounded and the landing page stays premium.

export interface LogHeadline {
  /** The short name of the thing that shipped. */
  title: string
  /** The kind label shown as a chip, matching KIND_META in changelog.ts. */
  tag: string
}

export const LATEST_LOG: LogHeadline[] = [
  { title: "A Treemap — the chart family's tenth shape, and the one none of the others is: the space-filling one. The donut and ring bend a share into an arc, the bar stands it up as a height, the waffle counts it out one square at a time — all of them leave the page mostly empty around the figure. A treemap refuses to, packing every share into one solid rectangle with no gap and no waste, each part a tile whose area is its value and whose label sits inside the part it names rather than off in a legend, so you read the parts by how much room each takes. The squarified layout d3 ships (Bruls, Huizing & van Wijk), written here by hand off a plain list of values — no charting library. Same colour discipline, kept honest for a chart that must carry text on its fills: one lime stepped by rank from a bright leaf on the largest tile down to a deep forest on the smallest, the label ink flipping dark-on-light or light-on-dark by each tile's own luminance so it reads on every step. The tiles scale in on scroll in rank order; hover or focus a tile or its legend row and it lifts to full lime while the rest dim, the readout naming its share. Debuts full-width in the Playground beside the Chord diagram, read live off the same library the waffle counts — the space-filling counterpart to the unit chart, on the same honest figures. Reduced motion sets every tile in place at rest, nothing growing.", tag: 'Component' },
  { title: "A Chord diagram — the chart family's ninth shape, and the one none of the others can be: the relational one. Every chart before it measured things in isolation — how much, how full, what share, a value at a crossing — but not what runs between them. This does only that. A set of groups is laid as arcs around a circle, each sized by how connected it is; then a quadratic-Bézier ribbon is woven through the centre for every pair that shares a tie, each end as wide as the tie is strong — the chord layout d3 computes, laid out here by hand from a symmetric matrix, no charting library. Same discipline as its siblings: one lime stepped by rank so the busiest rim segment reads brightest and the legend, not a second hue, says which arc is which, with ribbons borrowing their end's tone at a low alpha so the whole weave reads as one material rather than a rainbow of cables. The arcs draw themselves in clockwise on scroll and the ribbons fade the weave in behind them; the legend rows are real buttons, so hovering or focusing one lifts that group's arc and lights every ribbon it touches while the rest fall back, and the centre reads its share of all the links. Debuts full-width in the Playground beside the Heatmap on illustrative sample values — five facets of building a product — with a Shuffle that re-rolls the matrix and re-weaves. Reduced motion sets every arc drawn and the ribbons at their weave, nothing sweeping.", tag: 'Component' },
  { title: "Expanding panels — the gallery pattern the surface family was missing, and a calmer sibling to the deck, the fan, and the coverflow that share its shelf. A row where exactly one panel is open and grows to take most of the width — a plain flex-grow transition, so the reflow reads as one continuous motion — while its neighbours fold to slim spines carrying a rotated label and an index. Hover or focus a panel to open it and its title and body fade up under a lime wash with a hairline rule down the leading edge. Where the card stack recedes into depth and the fan spreads into an arc, this one simply widens: the quietest way to hold several things and pull one forward. Fully keyboard-driveable with one roving tab stop, the arrow keys walking the row and Home/End jumping to the ends; the default panels name the five ideas the Playground is built on, so the one that opens is a principle, not a pitch. On a narrow screen the row becomes a column and panels grow in height instead of width, so the spines never crush to nothing. Reduced motion keeps the layout reflow but drops it to an instant swap with no travel.", tag: 'Component' },
  { title: "A Ring chart — the chart family's eighth shape, and a turn back to product-grade data-viz after the run of generative physics. It reads what none of the others do: several independent proportions at once. The gauge gives one proportion a dial and the donut splits one whole into parts that must sum to it; this does neither, stacking three or four unrelated 'how full' readings as concentric rings — the radial-bar, or activity-ring, form. Same primitive as its siblings, an SVG circle whose visible fraction is drawn in through pathLength, each rotated to start at twelve o'clock and sweep clockwise, a bead riding every arc's leading edge; and the same discipline, one lime stepped by rank so the outer ring is brightest and the legend, not a second colour, says which ring is which. The legend rows are real buttons: hover or focus one and its ring lifts while the centre readout swaps to that measure and the rest dim. Debuts beside the gauge with a shuffle that re-draws every arc from zero. Reduced motion sets each arc at its final fraction, beads at rest, nothing sweeping.", tag: 'Component' },
  { title: "A Fluid — real ink on a real fluid, and the one continuous medium among the toys. Everything else there pushes discrete bodies around or reads a fixed field; this is a smoke that carries a swirl the way water carries a drop of dye. Jos Stam's stable fluids, the 1999 solver that stays stable at any timestep: a velocity field made divergence-free by a pressure solve each frame, then both the flow and the ink advected by tracing every cell back to where its contents came from. Two emitters lay curling ink on their own; drag to stir it into filaments, press to bloom a puff the flow runs away with. Reduced motion holds a settled swirl.", tag: 'Component' },
  { title: "A Card fan — a hand of cards held the way a card player holds them, and the tactile counterpart to the deck that recedes above it. Every card pivots around one point set well below the deck, so a single shared rotation fans them from a common base — overlapping at the bottom, spreading toward the top, the later cards laid over the earlier. Hover the hand and the whole fan opens wider and lifts; hover or focus one card and it climbs out of the fan, straightens toward upright, scales up, and its body unfolds from a whisper to full weight while the neighbours nudge aside. Honest about what it holds — the default hand is five facets of the site (craft, motion, type, range, built-in-the-open), so the card that comes forward is never one project but the range. Fully keyboard-driveable: one roving tab stop, the arrow keys walk the hand and raise each card, Home and End jump to the ends, all on one shared spring so nothing snaps. Reduced motion drops the fan for a calm, fully readable column with every card already open", tag: 'Component' },
  { title: "A Plucked string — a guitar string kept honest to its maths. Grab it anywhere and pull it aside and it bends into a clean triangle, pinned at both ends and held under your finger; let go and it rings — a wave races out from the kink to each fixed end, reflects, inverts, and crosses back through itself. That is the whole story, because a pluck is a triangle and a triangle is a sum of sine modes (Fourier), so what settles is the fundamental and its overtones beating against each other and damping at their own rates, the higher and brighter modes dying first, the slow fundamental last. The string is a row of nodes advanced by the textbook finite-difference solution of the 1-D wave equation — the leapfrog stencil, a Courant number kept under one for stability — run on a fixed-timestep accumulator, so pitch and ring-down read the same on any monitor and survive a resize untouched. The one-dimensional sibling of the Chladni plate: there the standing waves live on a square and draw themselves in sand, here on a line you pluck by hand. The line warms to lime where it moves fastest. Drag to pull it aside, tap to pluck it for you; reduced motion holds it in a single plucked triangle, still", tag: 'Component' },
  { title: "A Parallax diorama — a card with real inside. Where the tilt card rotates one flat plane toward the cursor, this stages a scene in layers and lets each respond to the pointer at its own depth: a diorama rather than a leaning surface. The default is a small, honest Berlin — a deep sky that barely stirs, a faint star field, a distant skyline, the Fernsehturm with a lit lime sphere, and the title plate closest of all. The card reads the pointer once and publishes the offset through context; every layer subscribes, so nearer planes drift further and against the cursor while the sky holds still, and each sits a little forward on the z-axis so the box separates and looks around itself as it tilts. Composition, not a photograph — three planes and a pointer, staged by hand. The layer primitive is reusable for any scene. Reduced motion collapses the tilt and the drift and rests every plane flat", tag: 'Component' },
  { title: "A Sticker peel — a lime sticker laid flush on the surface, and the one gesture that makes a sticker read as a sticker: the dog-ear. Grab the bottom-right corner and drag it toward the middle and it folds back like real vinyl — the flap shows its paper backface, a curl of light catches the crease, and the folded corner casts a shadow onto a line printed underneath, which reads through the gap the peel opens. The whole fold is one number and no image: the face is a pentagon clipped by a forty-five-degree corner cut that grows as you pull, and the flap is the mirror-image triangle it folds onto, both just clip-paths off one motion value. A drag sets the peel one-to-one; let go and a spring decides — past halfway it settles open, short of it snaps flat and re-adheres — and when idle it breathes a few percent so the corner reads as grabbable. The copy is a personal beat, not a client: built by hand in Berlin. Tap or press Enter to peel hands-free; reduced motion drops the spring, the breathing and the drag for a plain toggle", tag: 'Component' },
  { title: "A Times table — the single most surprising picture in elementary arithmetic, drawn as thread wound between pins. Space a few hundred points evenly round a circle and number them zero, one, two; then for a multiplier k run a straight thread from every point i across to point i times k, wrapping past the ring — that is, modulo the count. No curve is ever drawn, only chords, and yet the thread leaves a bright envelope: a cardioid at k of two, a nephroid at three, a chain of one-less-than-k cusps for every whole number after — the epicycloids a coin rolling round a coin would trace. Slide k off the whole numbers and the cusps unstitch and re-stitch. It reads as string art because it is: nails round a ring, one thread, a rule for where it goes next, tinted teal near zero and warming to lime round the ring, the chords laid in additively so their crossings glow. Left alone the multiplier climbs slowly through the whole numbers and back; drag across to drive k and up and down to add or thin the pins. Reduced motion settles one multiplier and paints a still figure", tag: 'Component' },
]
