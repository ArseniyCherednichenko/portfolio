// A compact, standalone list of the most recent build-log headlines, for the
// Home page's "In the open" section.
//
// Why its own tiny module and not derived from `changelog.ts`: the Home chunk
// ships eagerly, while the full Changelog page is lazy-loaded. If Home imported
// anything from `changelog.ts`, that (large) module would be shared with the
// eager entry and Rollup would hoist the whole log — every long entry string —
// into the initial bundle, undoing the code split. This file imports nothing
// heavy, so the headlines cost almost nothing up front.
//
// It mirrors the top of `CHAPTERS[0]` in `changelog.ts` — keep the two in step
// (the daily routine edits the log each run anyway). The full sentences, the
// kinds, and the filter all still live on `/changelog`.
//
// Kept to the ~10 most recent, as short one-line headlines: the Home section is
// a teaser ("the most recent of them") with a "Read the full build log" link,
// and every title here ships in the eager bundle — so this stays a tight,
// glanceable list, not the whole archive re-stated in full paragraphs. When you
// prepend a new entry each run, drop the oldest so the list does not grow
// unbounded and the landing page stays premium.

export interface LogHeadline {
  /** The short name of the thing that shipped. */
  title: string
  /** The kind label shown as a chip, matching KIND_META in changelog.ts. */
  tag: string
}

export const LATEST_LOG: LogHeadline[] = [
  { title: "A Parallel coordinates plot — the chart family's many-variables-at-once shape, and the one none of the others can be. Every chart before it reads a bounded number of dimensions: the bar a single height, the scatter exactly two, the candlestick four prices along one time axis. A parallel-coordinates plot reads as many numeric columns as you give it — each dimension its own vertical axis laid out left to right, each record a single polyline crossing every axis at its value there, so a whole many-columned table draws at once and the shape of the bundle is the reading: lines that run parallel agree, lines that cross between two axes trade off. The radar is also multivariate but wraps its axes into a ring; keeping the axes straight and open is what makes the signature interaction possible — brushing. Drag along any axis to keep only the records in that range, stack brushes across axes, and carve the bundle down to the ones high here and low there while the survivors light up. It keeps the family's rules: no plotting library, hand-laid SVG axes and polylines, each axis on its own round-number scale because the dimensions are in different units, and one lime stepped in lightness by a single chosen dimension so colour never encodes two things. The lines name all their values, the SVG carries a spoken summary that updates as you filter, and every record sits in a list beneath. Debuts in the Playground after the Sunburst on illustrative motion-preset ratings across five qualities in different units — not a benchmark — with brushing as the point rather than a reshuffle. Reduced motion sets every line at rest, and the brushing still works. The component count moves to 232.", tag: 'Component' },
  { title: "A Progressive blur — the honest version of the soft edge every floating header and scroll container reaches for. The lazy way, one backdrop-blur under a fading gradient, blurs everything by a single fixed amount and then only fades that layer's opacity, so text at the soft end is a crisp image dimmed — a ghost you can still read. A real one ramps the blur radius itself from zero to full across the band. This does it the proven way: a stack of full-size layers, each a little blurrier than the last, every layer masked so its opaque window slides one step further toward the edge; where the windows overlap the blur sums, so the seams vanish into one smooth climb, and the radii double as they go so the ramp reads even rather than bunched. It works on any of the four edges and is a passive overlay by design — pointer-events-none, aria-hidden — so it never eats a click or reaches a reader; drop it into any scroll box and that edge dissolves. Debuts in the Playground's scroll family on a real scrolling list, with a toggle to flip it off against the hard crop it replaces and one to deepen the ramp. No animation of its own, so reduced motion has nothing to switch off. The component count moves to 231.", tag: 'Component' },
  { title: "A hero worth landing on — a pass over the first thing anyone sees. The orbit on the right, once a couple of rings spinning forever regardless of anyone's motion setting, is now a proper constellation: a breathing core, a twinkling starfield, counter-rotating rings, and orbiting dots at two depths, the whole scene drifting with the cursor in real parallax and going completely still under prefers-reduced-motion. The same honesty swept the rest of the hero — the rotating word cross-fades instead of sliding, the scroll arrow and the corner seal's arrow hold still — so the opening screen no longer has a single loop that ignores the setting. And the line under the name, which had opened with the product, now leads with the person and the craft and lets Guided land at the end, one thing among several rather than the first word. Same facts, honest order; no new component, so the count holds at 230.", tag: 'Motion' },
  { title: "A password strength meter — the controls family had entry fields, the segmented passcode, the tag box, the number spinner, but never the one form control that has to talk back as you type, and the point of building it was honesty. Most strength meters are theatre: a coloured bar that turns green when you add a digit, backed by nothing you can see. This one shows its working. The estimate is a small, transparent entropy model — it counts which character classes are present to size the pool an attacker would have to search (26 lowercase + 26 upper + 10 digits + ~33 symbols), takes bits = length × log2(pool), then dampens that by how much of the string is actually distinct, so a repeated or tiny alphabet can't buy a strong score and 'aaaaaaaa' earns a fraction of what its length alone would suggest. The bit figure is printed right there beside the word band — no hidden score, no wordlist, no claim it can't back — and the five requirements tick over live as they are met, each check drawing itself in, while a toggle crossfades the value in and out. It is a plain labelled input with the band mirrored to a polite live region and the reveal button carrying its pressed state, so assistive tech hears the same story the eye does; nothing is submitted anywhere. Debuts full-width in the Playground's controls family beside the Star rating, with sample strings that move the bands. Reduced motion drops the staggered meter, the drawn checks, and the crossfade and leaves every reading legible. The component count moves to 230.", tag: 'Component' },
  { title: "A Sunburst — the chart family's fifteenth shape, and the one none of the others is: the radial-hierarchy one, the shape that reads a whole tree at once and reads it around. The donut bends a single flat share into one ring, the treemap packs a nested whole into rectangles, circle-packing nests it as bubbles; a sunburst nests it radially — the root is the still centre, its parts fan out as the first ring of wedges, and each part's own parts fan out again on the ring beyond, so angle carries the count and distance from the centre carries depth. And it zooms, which the flat charts have no room for: click a wedge that has parts of its own and it becomes the new centre, its children re-fanning to fill the whole circle so a small outer branch opens into its own full sunburst, a breadcrumb tracing the way back and the centre itself the way out. It keeps the family's rules — no plotting library, just hand-rolled SVG arc paths off a rolled-up tree, and one lime stepped not by identity but by depth, the ring nearest the centre a bright leaf and each ring out a step deeper into forest so hue only ever encodes how far down you are. A hovered or focused wedge lifts to full lime while the rest dim, the readout naming its share of the current centre. Debuts in the Playground after the Treemap, read live off the same library the waffle counts — the centre the whole set, the first ring its families, the outer ring every component as a thin wedge, one radial fingerprint of the catalogue. Reduced motion sets every wedge at rest, nothing sweeping open. The component count moves to 229.", tag: 'Component' },
  { title: "An Elementary automaton — the Wolfram one-dimensional cellular automaton, debuting at the head of the Playground's emergence family and, being a dimension smaller than everything beside it, its odd one out. The Game of Life, the Cyclic space, and the Sandpile all run a rule across a two-dimensional plane and change it in place; this runs the simplest rule there is on a single line of cells and stacks each new generation under the last, so the second dimension on screen is not space but time — the picture is the line's whole history, oldest at the top, and the shapes in it are what one plain law drew as it ran. The rule is as small as a rule gets: every cell is a 0 or a 1, its next value decided only by itself and its two neighbours, three bits and eight neighbourhoods, so a rule is just the eight-bit table saying what each becomes — exactly 256 of them, which Wolfram numbered by reading that table as binary. From so little, whole worlds: Rule 90 exclusive-ors its neighbours and draws the Sierpinski fractal from a single lit cell, Rule 30 is chaotic enough to have shipped as a random-number generator, Rule 110 was proved Turing-complete — that trivial three-bit lookup can, given the right starting line, compute anything any computer can. None of it is put in; it is only where the rule went, so the field cycles a curated tour and a chip names each rule as it takes over. A live cell leaves teal ink that stays as its row scrolls up into history while the newest rows glow lime-white, a bright advancing front laying down a record above it; press or drag anywhere to inject live cells straight into that edge and watch your own signal cascade up and evolve under the current rule. A single line evolves by integer lookups, the history scrolls with one copyWithin, and a grid-sized buffer is smoothed up — no wall clock, no per-frame randomness. Reduced motion computes one rule's full triangle top-to-bottom and paints it once. The component count moves to 228.", tag: 'Component' },
  { title: "The Studio — a new page, and the first that hands you something to take away. Pick a motif, a palette, a density, and a seed and it draws a generative poster straight onto a canvas in the browser; then a Download button re-runs the identical seeded drawing code onto a large offscreen canvas and gives you a crisp PNG — not a screenshot of the preview but the same composition at print resolution. The whole thing lives in the address bar, so a poster you like is a link that reopens it exactly on anyone's machine, because the seed alone decides the pixels (a mulberry32, never Math.random in the drawing). Three motifs to start — a phyllotactic bloom, streamlines through a seeded field, and beaded orbital rings — in four honest palettes, reachable from the command palette and the Home explore menu. The page count moves to 32.", tag: 'Page' },
  { title: "A Chaos game — a fractal precipitated out of pure chance, and the emergence toy the Playground was missing beside the diffusion crystal and the slime mesh. Its whole engine is one rule you can hold in your head: mark the corners of a regular polygon, drop a point anywhere, then over and over pick a corner at random and jump a fixed fraction of the way toward it, plotting where you land. Nothing in that rule mentions a shape and every choice is a fair coin, yet with three corners and a half-way jump the landings refuse to fill the middle and pile only onto a self-similar lace of ever-smaller triangles — the Sierpinski gasket, drawn by a process that has no idea it is drawing it: order is not put in, it falls out. It is an instrument, not a fixed figure — the corner count (three to six) and the jump ratio are yours, and each pair grows a different attractor; a square with a half jump is only noise, but forbid landing on the corner you just used and the same square grows a real fractal, so the no-repeats restriction is a control too. The randomness is seeded, a fixed mulberry32, so the same settings grow the same dust every load and it never touches Math.random or the wall clock; the picture is a real density map, each landing added with lighter compositing so where the walk lingers sums from lime toward white while the rare cells stay dark. The dust accumulates on its own persistent canvas over about a second and a half while a bright point skitters the walk on an overlay above it. Debuts full-width in the Playground after the Aggregate crystal, with the corners, the jump, and the no-repeats rule as real keyboard-drivable controls and a Recast that reseeds. Reduced motion plots the whole figure at once and holds it. The component count moves to 227.", tag: 'Component' },
  { title: "A Scatter plot — the chart family's fourteenth shape, and the one none of the others can be: the two-variables-at-once one. Every chart before it maps a single dimension to a position or a size — the bar a height, the donut a share, the candlestick four prices strung along one time axis. A scatter maps a point into a plane, an x and a y for every mark with no order imposed, so its subject is not 'how much' but 'do these two things move together' — the shape the cloud makes is the answer. That earns a reading none of the others need: a least-squares regression line, computed by hand from the covariance over the x-variance, drawn as the straight line closest to the cloud, with the r-squared it implies — how tightly the points hug that line, from 0 for no relationship to 1 for a perfect one — reported in plain words and a number. It keeps the family's rules: SVG circles and hairlines, no plotting library, one lime for the points and the line and the active ring, and a computed round-number axis on both sides, because a scatter floats in two dimensions and neither axis is forced to zero. The cloud dusts itself in point by point and the trend line then draws itself left to right; hover or focus a point (each a real focusable target) and a readout names its label and both coordinates while the rest dims, with every value listed beneath so the numbers never rest on the picture alone. Debuts full-width in the Playground after the Candlestick, on an illustrative cloud scattered around a rising line — not a real measurement — with a Reshuffle that re-scatters around a fresh line. Reduced motion sets the cloud and the line in place from the first frame. The component count moves to 226.", tag: 'Component' },
  { title: "Pathfinding — the search itself, laid bare, and the companion to the self-carving Maze it sits beside. A perfect maze has one route between any two cells, so a solver there has nothing to choose; this is the opposite, an open field of scattered walls where there are many ways across and the interesting thing is which cells a search bothers to look at on the way to the shortest one. Two real algorithms run on the identical grid and switching between them is the whole point. Dijkstra is uninformed: with no idea where the goal is, it spends the same effort in every direction, so its frontier grows as an even disc around the start — correct, thorough, wasteful. A* adds one thing, an estimate of how far each cell still is from the goal (the Manhattan distance, which never overestimates on a 4-connected grid, so the path stays guaranteed shortest), and spends its effort where that estimate is smallest, so its frontier is a tight cone leaning at the goal — the same path, a fraction of the cells looked at, and a live counter puts the difference in numbers. Both are the genuine algorithms driven by a hand-written binary min-heap, no library; the expansion order spreads as a lime stain with the leading edge bright and older cells cooled, and when the frontier reaches the goal the shortest path lights as one bright thread traced back through the recorded parents. Click or drag a cell to raise or clear a wall and the search re-runs instantly, so you can wall the cone off and watch A* bend around it; New walls reseeds a fresh scatter from a seeded PRNG so a grid is reproducible and the solve never touches Math.random. Debuts full-width in the Playground right after the Maze. Reduced motion drops the spread and the trace — the explored set and the final path are simply present. The component count moves to 225.", tag: 'Component' },
]
