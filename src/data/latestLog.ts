// A compact, standalone list of the most recent build-log headlines, for the
// Home page's "In the open" section.
//
// Why its own tiny module and not derived from `changelog.ts`: the Home chunk
// ships eagerly, while the full Changelog page is lazy-loaded. If Home imported
// anything from `changelog.ts`, that (large) module would be shared with the
// eager entry and Rollup would hoist the whole log — every long entry string —
// into the initial bundle, undoing the code split. This file imports nothing
// heavy, so the headlines cost almost nothing up front.
//
// It mirrors the top of `CHAPTERS[0]` in `changelog.ts` — keep the two in step
// (the daily routine edits the log each run anyway). The full sentences, the
// kinds, and the filter all still live on `/changelog`.
//
// Kept to the ~10 most recent, as short one-line headlines: the Home section is
// a teaser ("the most recent of them") with a "Read the full build log" link,
// and every title here ships in the eager bundle — so this stays a tight,
// glanceable list, not the whole archive re-stated in full paragraphs. When you
// prepend a new entry each run, drop the oldest so the list does not grow
// unbounded and the landing page stays premium.

export interface LogHeadline {
  /** The short name of the thing that shipped. */
  title: string
  /** The kind label shown as a chip, matching KIND_META in changelog.ts. */
  tag: string
}

export const LATEST_LOG: LogHeadline[] = [
  { title: "A day-rhythm dial for the Now page — a turn back to the person after a long run of abstract Playground pieces. The Now page is the site's most 'him, right now' surface, so it now opens with a live sense of presence rather than a static title: DayArc, a 24-hour day drawn as a sun arc — midnight at the two ends, noon at the apex — with a marker riding it at the live Berlin time and the stretch when I am likely at the desk and building lit in lime. The lit band is the exact 08:00–24:00 heuristic the shared useBerlinTime hook already uses for its 'awake' flag, so the dial can never disagree with the little status dots elsewhere on the site, and it is honest on its face: a typical rhythm, not a schedule or a promise. The geometry is a plain semicircle sampled into a polyline rather than an SVG arc with its flag guesswork, and the marker glides through the hour on the live minute; before 08:00 it reads hollow and 'likely asleep', after it fills lime and 'likely around'. The header becomes two columns on large screens — intro and dateline on the left, the dial on the right — and stacks on small ones. The snapshot itself was revised to today and its open-workbench figure moved from 'past two hundred' to 'past two hundred and thirty' hand-built components, keeping the now page honestly current. Under prefers-reduced-motion the marker's pulse comes off while its position still reads true.", tag: 'Component' },
  { title: "An Instant photo — a Polaroid, and the one gesture the cards-and-surfaces family was still missing: a picture that is not there yet. Where the flip card turns, the scratch card is rubbed away, the sticker peels and the ticket tears, this one develops — it mounts as a near-blank, over-exposed square and, over a few seconds, the image swims up out of the emulsion the way a real instant photo does: the contrast climbs, the colour floods back, the chemical fog burns off, the print settles. Nothing is faked in post — the whole develop is a single number from zero to one driving CSS filters (brightness, contrast, saturate, sepia, blur, opacity) over one canvas the scene is drawn to exactly once. The photo itself is honest: a seeded generative dusk — a graded sky, a low sun, layered hills, film grain and a vignette from a mulberry32 PRNG — abstract art that redraws identically each load, never a claim to be a photograph of a real place. And it carries the gesture people reach for: grab the print and shake it — every hard reversal agitates the emulsion, a burst of develop plus a wobble of the whole frame, so an impatient shake genuinely brings the picture up faster. Re-expose (click, or Enter once it is done) and it reseeds a new one-of-a-kind frame; while it is still developing, Enter and Space agitate instead. The frame tilts toward the cursor under a sliding gloss. Debuts full-width in the Playground's cards family after the direction-aware hover, two prints developing at once. Reduced motion drops all of it — the finished photo is simply there. The component count moves to 239.", tag: 'Component' },
  { title: "A Collatz coral — grown from the 3n+1 conjecture, a rule simple enough to explain in a sentence and stubborn enough that no one has ever proved it. Take any whole number: if it is even, halve it; if it is odd, triple it and add one; repeat. Lothar Collatz proposed in 1937 that every starting number, however wild its detour, eventually falls to 1 — and it is still an open problem. This is not a plot of those numbers but a drawing of the journeys: every path down to 1 is reversed so all of them begin at the same root, then walked as a line that bends a little one way on an even number and the other way on an odd one. Because thousands of sequences share the same ending — 16, 8, 4, 2, 1 — those shared tails overlap into one thick trunk near the root, and the paths only fan apart as they climb toward their distinct starting values, so the accumulation of every journey grows an organic, branching coral. Nothing is random: the same count always grows the same coral. Each strand is coloured only in lightness by its stopping time — a deep lime for the quick journeys, paling toward white for the long climbers. Two stacked canvases, one blooming the strands from the shortest journeys outward in an additive blend so overlapping paths gather into light, one for the hovered strand and its tip; DPR-capped, aria-hidden. Debuts full-width in the Playground's math family after the aperiodic tiling, with count and turn-style presets, a redraw, and a hover that follows one number's whole journey home — like 27, which climbs past nine thousand before it falls. Reduced motion paints the whole coral at once and holds it, and the hover still works. The component count moves to 238.", tag: 'Component' },
  { title: "A Hold to confirm — the press-and-hold button for the one-way actions a plain click fires too easily and a whole dialog over-serves: delete, wipe, publish. Hold it and a lime fill sweeps across; let go early and the fill rewinds instead of snapping, so a cancelled hold reads as a decision reversed, and a second press resumes from where it got to. The fill is one Framer Motion value animated over real elapsed time, so the sweep and the timer can never disagree. It is a real hold on the keyboard too — Space or Enter held down fills, the keyup cancels, auto-repeat ignored so a held key is one press, not a fresh one each frame — and a polite live region speaks only the outcome. Under reduced motion, with no fill to watch, it becomes a deliberate two-tap confirm that disarms itself if you look away, keeping the whole point with no motion at all. Debuts full-width in the Playground's controls family after the password meter, on a mock 'clear draft' with a short and a longer hold; nothing real is deleted. The component count moves to 237.", tag: 'Component' },
  { title: "A Direction-aware hover — the one hover the cards-and-surfaces family was still missing: a cover that knows which edge the cursor came from. The rest of the family each does one fixed thing — the glare card sweeps a single diagonal, the spotlight card glows under the pointer, the flip card turns on a hinge — and none reads intent. This one does: on the way in and out it takes the pointer's offset from the card centre, reads the angle with atan2, and quantises it to the four sides, so it knows whether you crossed the top, right, bottom, or left. That side picks an offset a whole card away, and a single spring carries the cover from there to rest on enter, then back out toward the edge you leave by — so a row of them feels shoved by the cursor rather than merely appearing. The cover is a real, always-rendered element, its copy selectable and reachable; keyboard focus slides it up from the bottom and blur sends it back, so it is not a pointer-only trick. Debuts full-width in the Playground's cards family after the expanding panels, a three-card deck naming disciplines rather than one project. Reduced motion drops the travel for a plain cross-fade. The component count moves to 236.", tag: 'Component' },
  { title: "An Aperiodic tiling — a Penrose tiling, the P3 rhombus form, and the one shape in the math family that fills the plane forever without ever repeating. Every other tiling has a unit cell you could slide onto itself; this one has none — shift it any distance in any direction and it never lines back up — yet it still covers the plane completely with just two tiles, a thick rhomb of 72 and 108 degrees and a thin one of 36 and 144. That is the astonishing part, order without repetition, and it is the picture behind quasicrystals, the matter whose discovery won the 2011 Nobel in chemistry. Nothing is placed by hand: the tiling is grown by deflation — a wheel of ten Robinson triangles meeting at the centre, then every triangle subdivided into smaller ones by the golden ratio, over and over (a thin half into a thin and a thick, a thick half into two thick and a thin), and because a subdivision that respects the matching rules can only grow the one legal tiling, the aperiodic order falls out of a rule you can hold in your head. The golden ratio is also the payoff you can read off the picture: thick rhombs over thin ones converges on phi, about 1.618, as it grows, and hovering a tile names it and prints that live ratio climbing toward the constant. Colour is one lime in two lightness steps, so hue only says which tile a piece is. Two stacked canvases, one accumulating the tiles as they bloom outward from the centre, one for the growth ring and the hovered rhomb; every vertex a deterministic golden-ratio construction, no randomness, DPR-capped, aria-hidden. Debuts full-width in the Playground after the road to chaos, with a depth stepper (3 through 7), a redraw, and the golden-ratio hover readout. Reduced motion paints the whole tiling at once and holds it, and the hover still works. The component count moves to 235.", tag: 'Component' },
  { title: "A Road to chaos — the logistic map's bifurcation diagram, and the one shape in the math family that plots not a trajectory but a destiny: for every growth rate r along the horizontal, the set of values the system finally settles into, stacked up the vertical. The rule is a single line, x -> r*x*(1-x), a toy population that grows on what is free and is culled by its own crowding. For small r it settles to one steady value; past r=3 that value splits in two, then four, then eight — the period-doubling cascade, each split faster than the last by Feigenbaum's constant (about 4.669) until, near r=3.5699, the doublings pile into a continuum: chaos, an infinitely fine spray of values with no period. Threaded through the chaos are sudden clear windows, the widest a clean period-3 orbit near r=3.83. Nothing is drawn by hand: each pixel column is one growth rate, the map iterated from x=0.5 with the first few hundred steps thrown away so the transient dies, the values it then keeps returning to plotted as it lands on them in the 'lighter' blend — brightest where it dwells, so the sharp lines are stable orbits and the haze is genuine chaos. The payoff is self-similarity: switch to the period-3 window and a whole miniature copy of the diagram appears inside it, the same structure at every scale. Hovering rings the settled values and prints the exact period under the pointer. Two stacked canvases, one accumulating the diagram column by column as it sweeps, one for the guide line and the orbit marks; no randomness, DPR-capped, aria-hidden. Debuts full-width in the Playground after the space-filling curve, with three range presets and a redraw. Reduced motion paints the whole diagram at once and holds it, and the hover readout still works. The component count moves to 234.", tag: 'Component' },
  { title: "A Space-filling curve — a Hilbert curve, the shape in the curve-tracing family with a paradox for a job: one continuous line, one-dimensional, that visits every cell of a two-dimensional grid without once crossing itself. It is built by pure recursion — order 1 is a plain U through a 2x2 grid, and every order after takes four copies of the one before, two of them rotated, stitched into the four quadrants, so order 7 threads all 16,384 cells of a 128x128 grid with one unbroken path. Nothing is placed by hand; every point comes from the standard bit-twiddling map between a distance along the curve and its cell, and its exact inverse. What makes it worth showing is locality: because the curve folds back on itself at every scale rather than scanning row by row, two cells near each other on the plane almost always sit near each other along the line too — which is why Hilbert order lays out images, databases and textures so things close in space stay close in memory. The colour reads the order of travel, a lime that lightens toward white as the distance grows, and hovering the field maps the cell under the pointer back to its place along the line, so a small move in space stays a small move along the curve until you cross one of the big folds. Two stacked canvases — one accumulating the path segment by segment, one for the glowing head and the hover ring — no randomness, DPR-capped, aria-hidden. Debuts full-width in the Playground after the Oscilloscope, with an order stepper, a redraw, and the locality readout. Reduced motion paints the whole curve at once and holds it, and the hover still works. The component count moves to 233.", tag: 'Component' },
  { title: "A Parallel coordinates plot — the chart family's many-variables-at-once shape, and the one none of the others can be. Every chart before it reads a bounded number of dimensions: the bar a single height, the scatter exactly two, the candlestick four prices along one time axis. A parallel-coordinates plot reads as many numeric columns as you give it — each dimension its own vertical axis laid out left to right, each record a single polyline crossing every axis at its value there, so a whole many-columned table draws at once and the shape of the bundle is the reading: lines that run parallel agree, lines that cross between two axes trade off. The radar is also multivariate but wraps its axes into a ring; keeping the axes straight and open is what makes the signature interaction possible — brushing. Drag along any axis to keep only the records in that range, stack brushes across axes, and carve the bundle down to the ones high here and low there while the survivors light up. It keeps the family's rules: no plotting library, hand-laid SVG axes and polylines, each axis on its own round-number scale because the dimensions are in different units, and one lime stepped in lightness by a single chosen dimension so colour never encodes two things. The lines name all their values, the SVG carries a spoken summary that updates as you filter, and every record sits in a list beneath. Debuts in the Playground after the Sunburst on illustrative motion-preset ratings across five qualities in different units — not a benchmark — with brushing as the point rather than a reshuffle. Reduced motion sets every line at rest, and the brushing still works. The component count moves to 232.", tag: 'Component' },
  { title: "A Progressive blur — the honest version of the soft edge every floating header and scroll container reaches for. The lazy way, one backdrop-blur under a fading gradient, blurs everything by a single fixed amount and then only fades that layer's opacity, so text at the soft end is a crisp image dimmed — a ghost you can still read. A real one ramps the blur radius itself from zero to full across the band. This does it the proven way: a stack of full-size layers, each a little blurrier than the last, every layer masked so its opaque window slides one step further toward the edge; where the windows overlap the blur sums, so the seams vanish into one smooth climb, and the radii double as they go so the ramp reads even rather than bunched. It works on any of the four edges and is a passive overlay by design — pointer-events-none, aria-hidden — so it never eats a click or reaches a reader; drop it into any scroll box and that edge dissolves. Debuts in the Playground's scroll family on a real scrolling list, with a toggle to flip it off against the hard crop it replaces and one to deepen the ramp. No animation of its own, so reduced motion has nothing to switch off. The component count moves to 231.", tag: 'Component' },
]
