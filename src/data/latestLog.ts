// A compact, standalone list of the most recent build-log headlines, for the
// Home page's "In the open" section.
//
// Why its own tiny module and not derived from `changelog.ts`: the Home chunk
// ships eagerly, while the full Changelog page is lazy-loaded. If Home imported
// anything from `changelog.ts`, that (large) module would be shared with the
// eager entry and Rollup would hoist the whole log — every long entry string —
// into the initial bundle, undoing the code split. This file imports nothing
// heavy, so the headlines cost almost nothing up front.
//
// It mirrors the top of `CHAPTERS[0]` in `changelog.ts` — keep the two in step
// (the daily routine edits the log each run anyway). The full sentences, the
// kinds, and the filter all still live on `/changelog`.
//
// Kept to the ~10 most recent, as short one-line headlines: the Home section is
// a teaser ("the most recent of them") with a "Read the full build log" link,
// and every title here ships in the eager bundle — so this stays a tight,
// glanceable list, not the whole archive re-stated in full paragraphs. When you
// prepend a new entry each run, drop the oldest so the list does not grow
// unbounded and the landing page stays premium.

export interface LogHeadline {
  /** The short name of the thing that shipped. */
  title: string
  /** The kind label shown as a chip, matching KIND_META in changelog.ts. */
  tag: string
}

export const LATEST_LOG: LogHeadline[] = [
  { title: "At a glance — the Home page's About section rebuilt to carry as much life as the sections around it. For a long run it was the thinnest block on the landing page: an eyebrow, one statement line, and a link — the one section actually about the person, and the emptiest. This run gives it a new Glance panel, a bento grid of honest facets read from one data file: where he is, what he works in, what he is building, that he is still a student, the way he works, and the site itself as an open workbench. Every facet is an established fact echoed elsewhere on the site, so it can never drift, and Guided is deliberately one facet in the middle rather than the headline — the section is about him, not one product. The whole grid is lit by a single lime spotlight that tracks the cursor across it (distinct from the per-card glow used elsewhere: one shared light, so the tiles read as one surface), each facet lifts and warms on hover, linked facets route into deeper pages, and one facet shows the live Berlin clock off the same hook the hero uses so the two never disagree. Reduced motion drops the spotlight, the lift, and the stagger while the clock still ticks — a fully legible panel with only the travel removed. A Home-section composition piece, not a catalogued component, so the count holds at 215.", tag: 'Polish' },
  { title: "An Oscilloscope — a cathode-ray tube in XY mode, and the honest counterpart to the Harmonograph and the Spirograph beside it in the Playground's curve-tracing family. Where those are pens that draw a figure and stop, this one holds no line of its own: the figure you see is a decaying afterimage of a point still moving. Feed the horizontal plates x = sin(fx·u + δ) and the vertical plates y = sin(fy·u), sweep the parameter u, and a single beam traces a Lissajous figure whose whole shape is set by the frequency ratio fx:fy — 1:1 an ellipse, 3:2 a trefoil, 5:4 a woven lattice. A whole-number ratio stands still; nudge one oscillator a hair off and the curve never quite closes, so it precesses — the standing figure slowly turning, which on a bench scope is what you watch for when two oscillators drift out of lock, and a slow deliberate detune keeps even a locked ratio gently rotating so it is never dead still. The persistence is real, not painted on: nothing is cleared between frames, each frame the whole face is dimmed by a thin wash of the background, and the new stretch of beam is added in the 'lighter' blend so overlapping passes bloom toward white the way excited phosphor does — the tail you see is the literal sum of how long ago each point was struck, brightest at the hot head still moving, with a faint graticule re-laid every frame so it holds a steady glow through the dimming. The pointer drives the oscillators: horizontal position walks the frequency ratio across a run of musical ratios, vertical sets the sweep speed and the detune, so a still hand holds a shape and a moving one walks it through the family — easing home to a slow idle trefoil when the cursor leaves. One canvas, one requestAnimationFrame loop off the clamped frame delta so a tab-away can't jump the sweep, DPR-capped and cleaned up on unmount. Debuts full-width in the Playground beside the Spirograph. Reduced motion never sweeps the beam: one closed figure at a whole ratio is drawn once as a steady glowing line and held, redrawn on resize.", tag: 'Component' },
  { title: "A Tesseract — the four-dimensional cube, and a turn away from both the generative shelf and the mechanical instruments the recent run has been feeding. It sits with the Spirograph and the Gear train in the geometry family, the honest oddity among them: where those trace a closed curve or hold a rigid ratio in a plane you can see, this holds a shape from a plane you cannot, and the whole point is to watch a 4-cube's shadow fold through itself. The object is exact, not evoked — 16 vertices, every choice of ±1 across four axes (x, y, z, w), and 32 edges, one joining each pair that differs in a single coordinate, so every vertex has four edges, one per axis. Those 16 points and 32 edges are built once and never change; all that moves is the frame they are read in. Rotation in 4D happens in a plane, not about an axis, and there are six of them; this turns three at once — the XW and ZW planes, the two that reach into the fourth dimension and make the inner cell appear to swell out through the outer one and back, plus a gentle XY spin so the whole figure also turns the familiar way, each a plain 2×2 rotation applied to its pair of coordinates. Then the casting down, twice: a 4D point is projected to 3D through a perspective divide by its distance in w — points nearer in the fourth dimension loom larger — and that 3D point is projected to the plane by the same divide in z, so nearness in either hidden dimension reads as size and brightness. Near edges are thick and near-white, far edges thin and dim, and the 'cube inside a cube' you always see in a tesseract diagram is just the far cell shrunk by that first divide. The pointer leans the tumble — the horizontal push speeds the XY spin while the vertical push drives an XZ tilt — both easing back to a slow idle drift when the cursor leaves, so it is calm on its own and steerable under your hand. Debuts full-width in the Playground's Objects & toys family, right beside the Gear train. One canvas, one requestAnimationFrame loop off the frame delta so a tab-away can't jump it, DPR-capped and cleaned up on unmount. Reduced motion never runs the loop: the figure is projected once at a flattering angle and held still, and the pointer does nothing.", tag: 'Component' },
  { title: "A Metronome — the pyramid-bodied mechanical instrument, and a deliberate turn away from the generative shelf the recent run kept feeding. It is the counterpart to the Euclidean sequencer already on the page: where that one composes a rhythm and sweeps a hand, this one does the single thing the wooden original does — hold a steady beat, set by a weight you slide up and down an inverted pendulum. Higher on the arm swings slower, lower swings faster, the same feel as thumbing the bob on a real Wittner. The swing is honest pendulum motion, not a linear sweep: one phase accumulator counts beats off the frame delta, and the arm angle is θmax·cos(π·phase), so the arm eases to a stop at each extreme and runs fastest through the centre exactly as an escapement does. Because tempo only ever changes how fast the phase advances, dragging the weight while it runs never jerks the arm — it just speeds up or slows down from wherever it is — and there is no wall clock anywhere, so a tab-away or a resize can't drift the count. Every way in works: drag the weight, press the steppers, hold the arrow keys on the focused control, or tap the tempo out on the pad and it takes the median of your last few taps. A time signature sets the beats to a bar and accents each downbeat, brighter and pitched up if you turn on the sound, which is off by default behind one lazily-built AudioContext. The weight is a real slider that speaks its value — '120 BPM, Allegro' — and the tempo term is read out as it crosses into a new range. Debuts full-width in the Playground's Objects & toys family, right beside the Euclidean ring. Reduced motion drops the sweep: the arm snaps to the side of each beat and the lights step forward, so the tempo still reads at a glance.", tag: 'Component' },
  { title: "A Code block — and, at last, the site showing its own source on the page rather than pointing at a repository elsewhere. Most of the recent run has grown the generative shelf — plasma, fractals, a frost crystal — so this is a turn back to the making itself, the same move the FileTree made. A syntax-highlighted code viewer, hand-built like everything else: no Prism, no Shiki, no highlight.js. The highlighter under it is a small single-pass TSX tokenizer written for this repo's own idiom — it walks the source once and knows comments, strings and template literals, numbers, keywords, function calls, JSX tag names, and Capitalised type and component names — deliberately a light highlighter, not a parser, which is exactly enough to make real TSX read clearly without pretending to understand the grammar. The palette never leaves the site's world: lime carries the language, a warm sand the strings, a cool teal the numbers, a pale lime the types and tags, near-white the calls, so a snippet reads as part of the page rather than a foreign theme dropped in. A header bar labels the file and a copy button hands over the exact source; the real text lives in the spans, so a screen reader reads the code and find-in-page still hits it, and the gutter numbers are unselectable so a drag-copy takes clean lines. In the Playground it renders Reveal.tsx verbatim — the scroll-into-view wrapper most of the site is built on — so the demo is a real file from this repository, not a toy snippet. On scroll in the lines settle up in a short stagger; reduced motion paints them at rest.", tag: 'Component' },
  { title: "A Frost crystal — diffusion-limited aggregation, the model behind soot, mineral dendrites, and the frost on a cold window (Witten & Sander, 1981), and the growth model the Playground's generative shelf was missing. The others grow a pattern out of something — two reacting chemicals, a trail thousands of agents share, grains toppling under one balance law, a rule you can hold in your head. This grows out of nothing but chance. A wall of seed sits along the bottom edge and single walkers drift down from above on a pure random walk: one that never touches the wall wanders off the top and is forgotten, one that brushes a frozen cell freezes on the spot and becomes wall for the walkers after it. Nothing chooses the shape — it is only where the random walks happened to land — and yet what grows is never a flat crust but the spiky, self-similar frost you know from a winter pane, because a tip that pokes up shields the valleys beside it and catches far more walkers, so tips race ahead and branch, fractal at every scale. Cheap like the fractals beside it: the aggregation lives on a coarse occupancy grid, walkers spawn just above the growth front so their walks stay short, and the plane wraps sideways so the frost is continuous across the width; every cell remembers the order it froze in, so the plane is painted ink at the old base up through lime to a near-white spark at the newest tips. The pointer leans the walkers' drift so the frost climbs taller under your hand; when the front nears the top it holds a beat, then thaws and begins again. Debuts full-width in the Playground between the Newton fractal and Langton's ant. Reduced motion grows one full crystal off a seeded generator and holds it, with no loop and no pointer.", tag: 'Component' },
  { title: "A Newton fractal — the third member of the escape-time family, and the one that asks the opposite question of the two beside it. The Julia and Mandelbrot sets ask whether an orbit flies off to infinity; this asks where an orbit lands. Newton's method is the schoolbook root-finder — stand at a guess, follow the tangent down to where it crosses zero, repeat — and run over the whole complex plane for f(z) = z³ − a, whose three roots sit evenly on a circle, every starting point is coloured by which root it converges to and how fast: three restrained tints — lime, a cool mint, a warm amber — ramped from ink through the tint to a white core where convergence is quickest. The whole beauty is the border: the three basins never meet along a clean line — between any two lies a filigree of the third, and between those more, fractal at every scale. The constant a rides a unit circle so the roots turn and the structure wheels and re-folds, and the pointer leans that phase to wind it forward or back under the cursor. Cheap like the plasma — a coarse offscreen buffer CSS upscales soft, the inner loop early-outs the moment a guess lands near a root. Debuts full-width in the Playground's fractal band. Reduced motion paints one already-formed phase and holds it.", tag: 'Component' },
  { title: "A Plasma field — the demoscene classic, rebuilt in the site's single accent. Four travelling sine surfaces — one running across, one down, one on the diagonal, and a radial ripple breathing out from the centre — are summed into one scalar field, and that value is mapped through a ramp from ink through olive to lime to a near-white specular, so the panel folds slow lime light rather than reading as a flat gradient. The trick that keeps it cheap is deliberate coarseness: the maths runs on a small offscreen buffer at one pixel per few screen pixels, then upscales onto the visible canvas with smoothing on, so the blocks melt into a continuous field while the inner loop only ever indexes a 256-entry colour table. Move across it and the pointer injects a ripple source that both distorts the interference and brightens the light where it passes — distinct from Iridescence's slow oil-slick wash and MeshGradient's blurred blobs, this one is a computed interference pattern. Debuts full-width in the Playground's field family. Reduced motion paints one still frame with no loop and no pointer.", tag: 'Component' },
  { title: "A NumberField — the native input type=number rebuilt from the ground up, and one more entry in the “hard native control, rebuilt” thread beside the Select, Combobox, Tag input, Calendar, Colour picker and Dropzone. The browser's spinner is one of the platform's quiet disappointments: two tiny arrows you cannot restyle, that look different on every browser, that only ever step by one, that jump the caret, and that have no press-and-hold and no way to drag a value the way every real editor lets you. This is a field you type into freely — it commits on blur or Enter, so a half-typed number is never yanked out from under you — with proper accelerating steppers you can hold, a grip you drag left and right to scrub the value, and full keyboard control: arrows step, Shift or the Page keys take the big step, Home and End jump to the bounds. Every value is clamped to its range, snapped to the step grid, rounded to its precision, and wrapped in a prefix and suffix, so the three fields in the demo stay honest as they feed one live total. It is a real role=spinbutton carrying a spoken aria-valuetext, so a screen reader hears “$1,200”, not “1200”. Debuts full-width in the Playground's controls family; the buttons squash on press and the grip warms and turns the cursor to a resize while you scrub, and reduced motion drops all of it and leaves the control identical to use.", tag: 'Component' },
  { title: "A file dropzone — the last control in the “hard native thing, rebuilt” thread and the crudest one the platform ships. input type=file gives you an unstyleable button and a bare filename: no drag target, no preview, no way to take a file back out. This is that control rebuilt as a real drop surface — a large, focusable target you can drag onto or press Enter to browse, that reads each file in the browser, previews the images as thumbnails, and lets you lift any one back off. Deliberately an interaction study, the same honesty the contact form and the waveform keep: nothing is uploaded and no backend exists, so files become object URLs on the page and are revoked again the instant a thumbnail leaves or the demo unmounts — the point is the interface, not a destination. The motion carries the weight: the dashed border and the whole surface lift and warm to the accent while a file hovers, each thumbnail springs in and folds out under an exit animation, and a single accent band sweeps a card as its file is read. Debuts full-width in the Playground's controls family, the natural close to the Select, Combobox, Tag input, Calendar and Colour picker before it. Reduced motion keeps every one of those a plain, instant state change, and it stays fully usable from the keyboard.", tag: 'Component' },
]
