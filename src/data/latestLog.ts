// A compact, standalone list of the most recent build-log headlines, for the
// Home page's "In the open" section.
//
// Why its own tiny module and not derived from `changelog.ts`: the Home chunk
// ships eagerly, while the full Changelog page is lazy-loaded. If Home imported
// anything from `changelog.ts`, that (large) module would be shared with the
// eager entry and Rollup would hoist the whole log — every long entry string —
// into the initial bundle, undoing the code split. This file imports nothing
// heavy, so the headlines cost almost nothing up front.
//
// It mirrors the top of `CHAPTERS[0]` in `changelog.ts` — keep the two in step
// (the daily routine edits the log each run anyway). The full sentences, the
// kinds, and the filter all still live on `/changelog`.
//
// Kept to the ~10 most recent, as short one-line headlines: the Home section is
// a teaser ("the most recent of them") with a "Read the full build log" link,
// and every title here ships in the eager bundle — so this stays a tight,
// glanceable list, not the whole archive re-stated in full paragraphs. When you
// prepend a new entry each run, drop the oldest so the list does not grow
// unbounded and the landing page stays premium.

export interface LogHeadline {
  /** The short name of the thing that shipped. */
  title: string
  /** The kind label shown as a chip, matching KIND_META in changelog.ts. */
  tag: string
}

export const LATEST_LOG: LogHeadline[] = [
  { title: "A Chaos game — a fractal precipitated out of pure chance, and the emergence toy the Playground was missing beside the diffusion crystal and the slime mesh. Its whole engine is one rule you can hold in your head: mark the corners of a regular polygon, drop a point anywhere, then over and over pick a corner at random and jump a fixed fraction of the way toward it, plotting where you land. Nothing in that rule mentions a shape and every choice is a fair coin, yet with three corners and a half-way jump the landings refuse to fill the middle and pile only onto a self-similar lace of ever-smaller triangles — the Sierpinski gasket, drawn by a process that has no idea it is drawing it: order is not put in, it falls out. It is an instrument, not a fixed figure — the corner count (three to six) and the jump ratio are yours, and each pair grows a different attractor; a square with a half jump is only noise, but forbid landing on the corner you just used and the same square grows a real fractal, so the no-repeats restriction is a control too. The randomness is seeded, a fixed mulberry32, so the same settings grow the same dust every load and it never touches Math.random or the wall clock; the picture is a real density map, each landing added with lighter compositing so where the walk lingers sums from lime toward white while the rare cells stay dark. The dust accumulates on its own persistent canvas over about a second and a half while a bright point skitters the walk on an overlay above it. Debuts full-width in the Playground after the Aggregate crystal, with the corners, the jump, and the no-repeats rule as real keyboard-drivable controls and a Recast that reseeds. Reduced motion plots the whole figure at once and holds it. The component count moves to 227.", tag: 'Component' },
  { title: "A Scatter plot — the chart family's fourteenth shape, and the one none of the others can be: the two-variables-at-once one. Every chart before it maps a single dimension to a position or a size — the bar a height, the donut a share, the candlestick four prices strung along one time axis. A scatter maps a point into a plane, an x and a y for every mark with no order imposed, so its subject is not 'how much' but 'do these two things move together' — the shape the cloud makes is the answer. That earns a reading none of the others need: a least-squares regression line, computed by hand from the covariance over the x-variance, drawn as the straight line closest to the cloud, with the r-squared it implies — how tightly the points hug that line, from 0 for no relationship to 1 for a perfect one — reported in plain words and a number. It keeps the family's rules: SVG circles and hairlines, no plotting library, one lime for the points and the line and the active ring, and a computed round-number axis on both sides, because a scatter floats in two dimensions and neither axis is forced to zero. The cloud dusts itself in point by point and the trend line then draws itself left to right; hover or focus a point (each a real focusable target) and a readout names its label and both coordinates while the rest dims, with every value listed beneath so the numbers never rest on the picture alone. Debuts full-width in the Playground after the Candlestick, on an illustrative cloud scattered around a rising line — not a real measurement — with a Reshuffle that re-scatters around a fresh line. Reduced motion sets the cloud and the line in place from the first frame. The component count moves to 226.", tag: 'Component' },
  { title: "Pathfinding — the search itself, laid bare, and the companion to the self-carving Maze it sits beside. A perfect maze has one route between any two cells, so a solver there has nothing to choose; this is the opposite, an open field of scattered walls where there are many ways across and the interesting thing is which cells a search bothers to look at on the way to the shortest one. Two real algorithms run on the identical grid and switching between them is the whole point. Dijkstra is uninformed: with no idea where the goal is, it spends the same effort in every direction, so its frontier grows as an even disc around the start — correct, thorough, wasteful. A* adds one thing, an estimate of how far each cell still is from the goal (the Manhattan distance, which never overestimates on a 4-connected grid, so the path stays guaranteed shortest), and spends its effort where that estimate is smallest, so its frontier is a tight cone leaning at the goal — the same path, a fraction of the cells looked at, and a live counter puts the difference in numbers. Both are the genuine algorithms driven by a hand-written binary min-heap, no library; the expansion order spreads as a lime stain with the leading edge bright and older cells cooled, and when the frontier reaches the goal the shortest path lights as one bright thread traced back through the recorded parents. Click or drag a cell to raise or clear a wall and the search re-runs instantly, so you can wall the cone off and watch A* bend around it; New walls reseeds a fresh scatter from a seeded PRNG so a grid is reproducible and the solve never touches Math.random. Debuts full-width in the Playground right after the Maze. Reduced motion drops the spread and the trace — the explored set and the final path are simply present. The component count moves to 225.", tag: 'Component' },
  { title: "A Candlestick — the chart family's thirteenth shape, and the one none of the others can be: the four-values-at-once one. Every chart before it reads a single number per mark — the bar a height, the area a point on a curve, the donut a share. A candlestick reads four at every mark: where a period opened, where it closed, and the high and low it touched between, all in one glyph and still read at a glance. It keeps the family's rules — no plotting library, SVG rects and hairlines, a computed round-number axis, one Framer Motion move — and keeps the single-hue discipline in the one place a candlestick usually breaks it. The convention is red-down / green-up, two colours telling direction by hue; the site's one-lime rule forbids that, so direction is told by FILL instead: a period that closed up is a solid lime body, one that closed down a hollow lime outline over the dark ground, so the single lime carries both the 'up or down' and the 'how far'. The axis is the other tell: a bar must start at zero because its length is the value, but a candle's position is the value, so the frame does not force zero — it hugs the data with headroom above the highest wick and below the lowest, the way a real price chart does. Each candle's wick and body grow out from its own centre-price on a staggered left-to-right reveal, like a tape printing; hover or focus a candle (each a real button over its whole column) and a readout floats above naming all four prices while a guide drops to the axis, with a visible O/H/L/C list beneath and a spoken summary of the opening price, closing price, and net move so the four numbers never rest on the picture alone. Debuts full-width in the Playground beside the Stream graph, on an illustrative walk — not a real price — with a Redraw that walks a fresh believable series. Reduced motion draws every candle at full extent from the first frame. The component count moves to 224.", tag: 'Component' },
  { title: "A Seven-segment readout — the digital-display thread now shows a number four ways, and this is the one everyone pictures when they say 'digital': the calculator, the alarm clock, the microwave. The odometer rolls number wheels, the split-flap drops Solari flaps, the dot-matrix lights a bitmap of round lamps; this lights seven fixed bars per cell and nothing else. Two details make it a real LED module rather than a blocky font, and both are easy to drop: the segments are stubby, mitred parallelograms with a gap at every seam, not clean strokes, and the unlit bars are still faintly there — the ghost of the whole figure-eight sitting behind a lone 1, which is the single most recognisable thing about the format. The limit is the character too: seven bars render every digit, a colon, and a decimal point exactly, then only the lopsided subset of letters they can honestly manage — the hELLO, the 0FF, the hex look — with anything else falling dark rather than faked. Pure inline SVG, no canvas and no font: every segment a hand-computed polygon, crisp at any size in both themes, a soft glow giving the lit bars real bloom. When a cell's value changes its bars cross-fade, a lit one easing on and a dropped one easing off — the tick that makes a running clock feel alive without layout thrash — and reduced motion turns the cross-fade off and cuts. The figure is aria-hidden and the string rides in the wrapper's label, so a reader hears '12:04', not seven bars. Debuts full-width in the Playground's display family, keeping the live Berlin time, with a field and sample chips to drive your own value and see what seven bars can and can't spell. The component count moves to 223.", tag: 'Component' },
  { title: "A Dot-matrix sign — the type family had a dozen ways to move letters, but every one set them in a real typeface first and then animated the result; this is the one piece with no typeface at all. A dot-matrix sign, the kind over a train platform, does not typeset its words — it lights them, one lamp at a time, on a fixed grid of dim LEDs. So the character set is a hand-authored 5x7 bitmap: uppercase, digits, and the punctuation a sign needs, each glyph written out dot by dot, no web font. The scroll is the tell — a DOM marquee slides text smoothly and reads as text sliding, but a lit dot is a lamp at a fixed position, so a real board can only step its message sideways one whole dot-column at a time, which is exactly what this does, and it reads as lamps switching on and off rather than text sliding. One canvas and a single loop that only repaints when the column advances, so a slow, readable board costs a handful of paints a second, not sixty; DPR clamped, grid rebuilt on resize, lit dots blooming softly over the dim ones. The canvas is aria-hidden and the live message rides in the wrapper's label, so a reader hears the words. Reduced motion never starts the loop — it lights the message from its first column and holds it still. Debuts full-width in the Playground's type family, with a field to light your own message and a slider for the crawl. The component count moves to 222.", tag: 'Component' },
  { title: "A Stream graph — the chart family's twelfth shape, and the one none of the others can be: the many-over-time one. The area chart rises a single line from a floor; the donut and waffle fix a part-to-whole at one instant; the Sankey moves an amount between stages. None of them shows several quantities changing together over time, which is all a stream graph does: each series is a band whose height at any point is its value, and the whole stack floats on a free baseline rather than pinned to zero. That baseline is the character of the form — pin a stacked area to zero and every upper layer rides the sum beneath it and wobbles for reasons that aren't its own, so instead this uses the wiggle-minimising baseline from the ThemeRiver work (Byron & Wattenberg), which pushes each column's stack down by a weighted sum of its layers so the river settles around its centre and every band keeps its own shape. That offset is computed here by hand from the values, no charting library, on one shared scale fixed off the tallest column so the bands compare across the run, with Catmull-Rom splines on every edge so the river flows rather than folds. The whole thing plots itself in one left-to-right wipe — the same reveal the area chart draws with — and reduced motion sets it full width from the first frame. Same discipline as the family: one lime stepped by total so the busiest band reads brightest, the legend carrying identity so a second hue never has to; the legend rows are real buttons, so hover or focus lifts a band while the rest fall back, with a readout naming its total and a full screen-reader summary walking every series. The flow shown is illustrative, not a claimed figure — five kinds of making, their emphasis shifting across six chapters — with a Reweight that re-rolls every value from a smooth random walk so the bands re-flow into a new but still organic shape. Debuts full-width in the Playground beside the Sankey. The component count moves to 221.", tag: 'Component' },
  { title: "A Sankey — the chart family's eleventh shape, and the one none of the others can be: the directed one. The bar, donut, gauge, and waffle each measure a quantity where it sits; the chord measures what ties two things together but never which way it runs. A Sankey measures movement itself — how a quantity splits from one stage, travels, and re-gathers at the next. Nodes fall into columns by how far along the flow they land (a longest-path layering computed from the link list), stacked to a shared value scale so bars compare stage to stage; a band leaves each node as wide as the amount it carries, curves across the gap on two cubic edges, and lands exactly that wide, so width is conserved the way real throughput is — the layout d3-sankey would hand you, laid out by hand, no charting library. Same discipline as the rest of the family: one lime stepped by rank so the busiest node reads brightest, the columns and legend carrying identity so a second hue never has to, every band borrowing its source's tone at a low alpha so a flow reads as one material rather than a tangle of cables. Bars grow from their centre column by column on scroll, then the bands fade the weave in behind them; nodes and legend rows are real buttons — hover or focus one and it lifts while every band running through it lights and the rest fall back. The flow shown is illustrative, not a claimed figure: how the effort on a typical build fans from an idea through the disciplines into what ships, with a Reweight that re-rolls the values so the bands re-flow. Debuts full-width in the Playground beside the Chord diagram. Reduced motion sets every bar and band at rest, nothing sweeping.", tag: 'Component' },
  { title: "A Star rating — the control the platform never shipped. HTML has an input for a number, a colour, a date, a range, but never a rating, so every product hand-rolls one and most stop at clicking a whole star: no half steps, no keyboard, no reward for the tap. This is that control done properly. It is a real slider, and the important word is one — the whole strip is a single tab stop rather than five separate stars, the way a native slider is one control, so the arrow keys nudge it by half a star, Page keys move a whole one, Home and End jump to the bounds, a number key sets that many, and 0 clears it, with a spoken value ('3.5 out of 5 — Great') so a screen reader hears the meaning rather than a bare digit. The pointer previews as it moves, the fill following the cursor and snapping to the near or far half of the star under it, and commits on click. Half steps are handled honestly — each star is a plain empty outline with a lime fill laid over it inside an overflow box whose width is the fill fraction, so a half star is only ever cropped, never squashed. The reward is the point: on commit the filled stars pop on a staggered spring and a small ring of lime sparks bursts from the last one, the tiny moment of delight a rating is meant to earn and almost never gets — deterministic, so it reads the same every time. Debuts full-width in the Playground beside the Number field — 'the control the platform ships badly' next to 'the control it never shipped' — with an interactive rating above and a read-only display below. Reduced motion drops the flourish and simply lands the fill; the control stays exactly as usable.", tag: 'Component' },
]
